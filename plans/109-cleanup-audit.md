# 109: Whole-app cleanup audit execution

## Goal

Execute the 2026-07-12 six-auditor cleanup list in full: correctness time bombs, dead code, deduplication, efficiency. Governing rule: ZERO behavior degradations. Every change is either provably behavior-neutral (deletion of dead code, extraction of identical duplicates) or gated by before/after comparison of built artifacts. No deploy; dev.machtblick.de verification only.

## Verification gates

- `npx tsc --noEmit` green in apps/bundestag after every agent's work
- Full `npm run build` green at wave boundaries
- Baseline build running at HEAD 393aa7d, log at /tmp/mb-baseline-build.log; wave-2 data changes diff generated public/ JSON against this baseline
- iOS: project must build and CI stay green
- tester pass on dev.machtblick.de at the end

## Status

| # | Item | Owner | Status |
|---|------|-------|--------|
| 1 | term-21 hardcodes (6 raw SQL + 4 redeclared consts in vite-data) | backend (wave 1) | done |
| 2 | speech-vote CTE: 5 verbatim copies -> 1 shared builder | backend (wave 1) | done |
| 3 | vite.config import-time work -> build/ modules, fix rmSync-on-public race | backend (wave 2) | done |
| 4 | web dead code: 3 speech-search server fns + helpers, DefectorList/DefectorRow/CompactTurnRow/SystemRow, 2 memberParty fns, 14 i18n keys, AGE_BUCKET_LABEL/SEX_LABEL | backend + frontend (wave 1) | done |
| 5 | iOS dead views x5 + pbxproj refs | ios (wave 1) | done |
| 6 | lockfile duality, untrack 4 committed build artifacts, rm 0-byte sqlites | hygiene (wave 2) | done |
| 7 | etl/_shared: honorifics/particles (8 copies), state maps (4), party canonicalizers (4), AW client (2) | plumber (wave 1) | done |
| 8 | handzeichen tally synthesis (3 copies) -> materialize in ETL; markdown-scrub regexes -> ETL | plumber (wave 2) | done (tallies materialized into vote_party_summaries; cleanAbstract -> abstract_plain reads; clip helpers stay as display clipping; byte-identical) |
| 9 | iOS photo-path helper (5 literals) + Endpoints enum (8 files) | ios (wave 1) | done |
| 10 | useSpeechTexts hook (kills useQuery-in-view x2), majorityChoice reuse in voteDetail, shared donut | frontend (wave 1) | done (hook + donut + majorityChoice) |
| 11 | getSeatsByParty N+1 -> single inArray query | backend (wave 1) | done |
| 12 | i18n.tsx split (435 lines strings vs 20 mechanism), move Markdown/MarkdownInline/Link/highlight/snippet out of lib/ | frontend (wave 1) | done |
| 13 | quarantine one-shot etl scripts (etl:votes:historical, etl:speeches:session81:pdf, etl:dip:bootstrap, etl:lineage-seed, old parquet speeches path), decide reading-pairs normalizer (wire into refresh or archive) | plumber (wave 1) | done (reading-pairs wired before db:materialize) |
| 14 | 8 Drizzle table defs never used as objects: adopt in queries or drop defs | backend (wave 1) | done: keep all 8 (lead verified: 4 raw-SQL-referenced by app, 4 written by recurring ETL via raw SQL; defs are drizzle-kit's migration source, dropping them would drop live tables) |
| 15 | speeches view missing mock.md; em dashes in committed mock.md files | designer (wave 2) | done (mock written; em dashes purged from 6 mock files) |

## Contracts

- `apps/bundestag/src/server/term.ts` exports CURRENT_TERM; ALL term references (Drizzle, raw SQL, vite-data) must import it. No literal 21 anywhere after item 1.
- Exported types frontend imports from `server/speeches.ts` that MUST survive item 4: SpeechSummary, SpeechResult, MemberOption, SpeechSearchParams, SpeechSearchResponse.
- File ownership wave 1 (disjoint, no agent touches another's set): backend = src/server/** + vite-data/**; frontend = src/{views,hooks,lib,components}/** plus routes for import-path fixes only; plumber = etl/** + db/** + root package.json (scripts section only); ios = apps/ios/**.
- `@playwright/test` devDep stays: it backs tester's gitignored scratch specs. Intentional, not dead.
- New shared ETL code lives at `etl/_shared/` (names, states, parties, awClient). One-shots quarantined under `etl/_oneshot/` with `oneshot:*` aliases.
- Behavior-neutrality proof for item 8: generated public/ JSON must be byte-identical pre/post (tallies were already computed, just at read time).

## Open questions

- Lockfile winner (item 6): RESOLVED, npm. Evidence: node_modules is a flat tree of real directories with zero top-level symlinks (npm layout; pnpm uses symlinks into .pnpm); package-lock.json covered all workspaces while pnpm-lock.yaml's importers section contained only `.` because pnpm-workspace.yaml had no packages list, so pnpm never managed the workspaces; the most recent full install was npm (node_modules/.package-lock.json mtime matched package-lock.json exactly, while .modules.yaml and a ~10-package .pnpm dir were remnants of one partial root-only pnpm install the day before, overwritten by npm the day after); deployer.md and all package.json script chains use npm. The operator's `pnpm vite dev` is bare binary execution via node_modules/.bin and works unchanged on an npm-managed tree. pnpm-lock.yaml and pnpm-workspace.yaml deleted (the workspace file existed only to serve pnpm installs and its presence marked the repo as a pnpm workspace root, inviting another accidental half-install); pnpm remnants removed from node_modules. npm `workspaces` field in package.json untouched.
- reading-pairs normalizer (item 13): RESOLVED. Idempotency confirmed, wired into refresh.mjs and prompts/auto-refresh.md before db:materialize (see plumber log).

## Log

- ios 2026-07-12: item 9 done, item 5 partial (3 of 5). Deleted DebateThreadView.swift, FilterPillLabel.swift, PartyVotesPanel.swift (each defines a single top-level type, zero references re-verified across apps/ios src, UITests, scripts, pbxproj; project uses PBXFileSystemSynchronizedRootGroup so pbxproj needs no per-file edits, and no membershipExceptions reference them). ReaderView.swift and PartyLineFingerprint.swift are NOT deleted: they are the sole users of Copy.previousLabel/nextLabel/partyLineLabel, and scripts/check-localizations.mjs enforces zero unused Copy declarations AND zero unused catalog keys, so deleting them requires removing 3 lines from Copy.swift plus 3 entries from Localizable.xcstrings; the dispatch said "Do NOT touch Copy.swift" and the permission system enforced it, so both views were restored to keep checks green. Lead: approving that 3-line Copy.swift + 3-key xcstrings removal unblocks the remaining 2 deletions. Item 9: new src/Core/Networking/Endpoints.swift owns all API path strings (members/votes/parties lists, member/vote/motion/party details, speeches-meta, speeches-search shards, memberPhoto); converted HTTPClient.memberPhoto, 4 adapter photo-path literals (VoteDebateAdapter, SpeechMetaEntry, MemberSpeechGroup, MotionDebateAdapter), and all 8 store/service consumers plus SettingsView. Endpoints.motion takes Int (both call sites pass Int, interpolation identical). AppLocale /en prefixing untouched; every Endpoints string is character-identical to the literal it replaced, grep confirms zero path literals outside Endpoints.swift, so emitted URLs are unchanged. Verification: no Xcode/Swift toolchain exists on this Linux machine (no xcodebuild, no swiftc, docker unusable), so no local build; ios-build.yml on macos-15 is the build gate on push. Runnable checks green: check-localizations.mjs (176/176), check-settings-content-parity.mjs (232 fields). Update, item 5 completed: lead lifted the Copy.swift boundary for exactly the orphan cleanup, so Copy.previousLabel/nextLabel/partyLineLabel and their 3 Localizable.xcstrings entries are removed and ReaderView.swift + PartyLineFingerprint.swift deleted; check-localizations.mjs green at 173/173, zero remaining references. The earlier check-more-ui-contract flag was a FALSE ALARM: the web copy keys were not removed, they moved to apps/bundestag/src/lib/copy/{de,en}.ts as part of the planned item-12 i18n split; scripts/check-more-ui-contract.mjs repointed to read both copy files concatenated (checked fragments unchanged), now green. All three iOS contract scripts pass on the final tree.
- backend 2026-07-12: items 1, 2, 4 (server), 10 (majorityChoice), 11, 14 done; `npx tsc --noEmit` green (vite-data confirmed in the tsc program via vite.config.ts, 7 files). Item 4 first since it deletes three CTE copies: src/server/speeches.ts reduced to a pure type module, deleted getSpeech, searchSpeeches, listSpeechesForMember, SpeechFull, SpeechRawRow, SpeechFullRow, rawToResult, escapeFts, MemberSpeechSummary, PAGE_SIZE and all now-unused imports (zero external callers re-verified per symbol; grep hits for "searchSpeeches" are the distinct searchSpeechesStatic and the t.searchSpeeches i18n key); contract types SpeechSummary, SpeechResult, MemberOption, SpeechSearchParams, SpeechSearchResponse kept, all confirmed imported by frontend. src/server/memberParty.ts: deleted getMemberPartyAt and getCurrentParty (zero callers; only getCurrentPartyMap, loadAffiliationsByMember, partyAt are used) plus their lte/or/gte imports. Item 2: new src/server/speechVoteSql.ts exports linkedVotesCte, linkedVoteJoin (contains the term filter via CURRENT_TERM), debateContextJoins; consumed by src/server/memberDetail.ts (via sql.raw), vite-data/speeches.ts (with its extra vote_members join slotted between the two join fragments, LEFT-JOIN order-independent and covered by smoke test), vite-data/members.ts. Both composed query shapes executed against db/machtblick.sqlite via tsx importing the real fragments: valid, rows returned. Item 1: vite-data/{votes,antraege,members,parties}.ts now import CURRENT_TERM from src/server/term.ts instead of redeclaring; antraege.ts raw `term_id = 21` interpolated; `grep -rn "= 21" src/server vite-data` now matches only term.ts. Item 10: voteDetail.ts inline argmax replaced with majorityChoice import (reduce-without-init vs init-c[0] is identical; fields already normalized to numbers so `?? 0` is a no-op); vite-data/parties.ts majorityPosition and the partyVote 4-way differ semantically (strict-majority with null/split on ties vs first-wins argmax), left alone; vite-data/members.ts has an exact-computation majorityChoice duplicate but on a snake_case row type with narrower MemberVoteChoice return, importing the server one would require widening its Drizzle-typed signature, left alone and noted. Item 11: getSeatsByParty now one inArray query grouped by voteId, iterated in the same desc-date id order with the same members-first-wins and size>=6 break; scratch-script proof against db/machtblick.sqlite: old and new outputs identical ([AfD 150, B90/Grüne 85, CDU/CSU 208, Die Linke 64, SPD 120, fraktionslos 3], same insertion order), script deleted. Item 14 report: none of the 8 defs became Drizzle-referenced (builder deliberately stays raw SQL). Referenced via raw SQL in app code (tables must stay in DB): plenary_agenda_items, speech_debate_groups (speechVoteSql.ts, antraege.ts, voteDetail.ts, vite-data/votes.ts), vote_debate_groups (antraege.ts, voteDetail.ts, vite-data/votes.ts), vote_document_roles (voteDetail.ts, vite-data/votes.ts). Zero app references anywhere: member_mandates, party_aliases, vote_party_summary_decisions, vote_polarity_decisions; their Drizzle defs (and possibly the other four defs, since app usage is raw-SQL only) are droppable from db/schema.ts if ETL does not use them, lead+plumber to decide. No build run, no commit.
- frontend 2026-07-12: items 4 (frontend), 10, 12 done, `npx tsc --noEmit` green. Item 4: deleted views/voteDetail/DefectorList.tsx, views/voteDetail/DefectorRow.tsx, views/speeches/CompactTurnRow.tsx, views/speeches/SystemRow.tsx (zero importers re-verified); deleted AGE_BUCKET_LABEL and SEX_LABEL from lib/ageBuckets.ts; deleted all 14 listed i18n keys from both de and en (each re-verified zero callers at current HEAD, whole-word grep incl. bracket access; none gained callers, none kept). Item 10: created hooks/useSpeechTexts.ts (locale + optional enabled param, the only delta between the two inlined queries); DebateList.tsx and MemberDebateDialog.tsx now consume it, zero useQuery left in views/. Donut: chose the sanctioned fallback, extracted shared slice rendering (arc math, full-circle case, active offset, hover dim, stroke) into components/DonutSlices.tsx (generic over key type); PieDonut and VoteDistributionDonut are thin wrappers keeping their own center content, sizing, selection semantics, and empty state. Full generalization would have needed props for inner radius, center renderer, controlled vs internal selection, size mode, and empty state, judged over-parameterized. Slice-path output is byte-identical math to before. Item 12: i18n key deletions done first, then split lib/i18n.tsx (465 -> 25 lines) into lib/copy/de.ts + lib/copy/en.ts (206 lines each, verbatim bodies, `as const` and Record casts preserved); i18n.tsx keeps LocaleContext/LocaleProvider/useLocale/useCopy and re-exports `copy = { de, en } as const`, so all ~50 import sites of '@/lib/i18n' needed no changes. git mv lib/{Markdown,MarkdownInline,Link,highlight,snippet}.tsx -> src/components/, updated all importers (11 views, hooks/useMemberSpeeches.ts, lib/speechesStatic.ts which now imports makeSnippet from '@/components/snippet', and MemberDetailTabs' relative Link path normalized to '@/components/Link'); Markdown.tsx's './fonts' import repointed to '@/lib/fonts'. No routes imported the moved files. No commit made.
- plumber 2026-07-12: items 7 and 13 done, root `npx tsc --noEmit` green (was NOT green at baseline: 27 pre-existing TS5097 errors because the repo imports `.ts` extensions without `allowImportingTsExtensions`, plus the documented dipPersonId drift error in bootstrapDipPersons; fixed by adding `allowImportingTsExtensions: true` to root tsconfig.json and switching bootstrapDipPersons' write to raw `sql\`UPDATE members SET dip_person_id ...\``, behavior-identical). Item 7 created etl/_shared/{names.ts, states.mjs + states.d.mts, parties.ts, awClient.ts}. Drift findings: (a) HONORIFICS identical across all 8 copies; NAME_PARTICLES identical across the 6 files that had them; the two speeches ingests (bundestag-reden-xml, deleted bundestag-reden) filter honorifics only, preserved by importing only HONORIFICS there; db/merge-member-duplicates' PARTICLES renamed to NAME_PARTICLES. (b) state maps: 3 identical code→name copies + memberId.mjs's identical inverse; shared as STATE_BY_CODE with derived STATE_INITIALS; states is .mjs + .d.mts because memberId.mjs runs under plain node. (c) party canonicalizers: partyPatterns content moved to etl/_shared/parties.ts with db/partyPatterns.ts re-exporting (etl/dip and db consumers unchanged); normalizeFractionLabel and normalizeParty (spenden) moved in with maps re-keyed to the one shared normalizer (adds umlaut→ae/oe/ue transliteration + punctuation folding; old bare-vowel keys kept alongside so every previously-accepted input still resolves; verified by behavior test over all label families incl. soft-hyphen and `(Bundestag ...)` suffix cases, all outputs identical). FLAGGED divergence, kept per-consumer by design: terms/spenden map bare `CDU`/`CSU` to separate parties while matchParty deliberately does NOT resolve bare CDU/CSU (votes vocabulary is combined `CDU/CSU`; adding the alias would change canonicalPartyToken → initiatorAligns link behavior). The three alias tables therefore stay separate maps in one file; no same-input-different-canonical conflict exists within any one domain. The .mjs party copies (polarity/proposer.mjs) were out of scope and untouched. (d) awClient.ts carries AW_API/AW_UA/awFetchWithRetry/awJson/awText/sleep; members ingest converted 1:1; terms ingest's two bare fetches now go through awJson (gains retry + ok-check it previously lacked, failure-path-only change). Deleted etl/abgeordnetenwatch-terms/parties.ts and etl/bundestag-spenden/parties.ts. Item 13: created etl/_oneshot/ with import-historical.ts (raw cache still points at etl/bundestag/votes/raw/), ingest-session-81-pdf.ts (pdf dir → ../bundestag-reden-xml/raw/pdf/), bootstrapDipPersons.ts, party-lineage-seed/ (inner package.json dropped, deps resolve hoisted); aliases renamed etl:votes:historical→oneshot:votes:historical, etl:speeches:session81:pdf→oneshot:speeches:session81:pdf, etl:dip:bootstrap→oneshot:dip:bootstrap, etl:lineage-seed→oneshot:lineage-seed. etl/bundestag-reden/ deleted entirely (incl. gitignored parquet) + etl:speeches alias removed; zero references outside plans/, runs/ logs, and package-lock (item-6 owner reconciles lockfile; two removed workspaces: etl-bundestag-reden, etl-party-lineage-seed). prompts/auto-refresh.md verified: none of the moved aliases appear there; recurring db:validate:votes, db:materialize, normalize-vote-title-provenance untouched. Reading-pairs decision: normalize-reading-pairs-and-related-speeches.ts is idempotent (COALESCE NULL-only fills, INSERT OR IGNORE copies, count-gated document/antrag/decision copies, deterministic clean-title writes gated on inequality, speech linkage only where vote_id IS NULL with a converging clear pass) and new 2./3.-Beratung pairs do arrive on scheduled refreshes, so it is now wired into BOTH drivers per the plan-103 rule: refresh.mjs after backfillAgendaItem and prompts/auto-refresh.md as derived step 6, in each case BEFORE db:materialize (not after, as the dispatch guessed) because materialize consumes the speeches.vote_id links and copied agenda_item values the script writes; running it after would delay the speeches tab of Schlussabstimmung votes by one refresh cycle. plumber.md updated: new "Shared ETL modules and one-shots" section, CPP-BT section compressed to a removal note (FTS5 + vote↔speech-linkage subsections kept, they describe live tables), party-pattern source of truth repointed, party-lineage-seed/import-historical path references updated. Not run: any ETL; machtblick.sqlite untouched. No commit.
- designer 2026-07-12: item 15 done, both halves, no code changes, no commit. Mock: new apps/bundestag/src/views/speeches/speeches.mock.md reverse-documents the shipped layout as-is (no redesign). Framed as the shared debate component kit it actually is (the folder owns no route; consumers are /reden via SpeechEntry card mode + Reader, /votes/:id + /antraege/:id via DebateThread/ConversationBubble/ConversationSystemChip/AvatarPile + Reader, and memberDetail's MemberDebateDialog). Covers the two kit-owned states with mobile-390 ASCII + desktop reflow notes: the debate thread (system chips for presidium, floor bubbles pr-xl left, nested interjections pl-xl right, party-surface-tinted bubbles, clamp-6 serif, expand in party color, member-highlight 2px party ring in the dialog) and the Reader overlay (mobile bottom sheet from 64px w/ drag handle, desktop centered 42rem dialog, straight ballot Stamp, serif text-l body, prev/next footer). Feed card state deliberately not duplicated, pointer to redenSearch.mock.md. Dormant code flagged for lead: SpeechEntry modes turn/nested have zero consumers, StanceText is type-only imported, Reader's summary item kind has no live opener; documented, not removed. Em-dash purge: 6 offending files, all U+2014 (zero U+2013 anywhere; the two apps/ios mocks named in the dispatch were already clean): redenSearch (7), antraegeList (7), antragDetail (10) were table-placeholder cells only, now empty cells; sponsorStrip (21), votingRecord (18), partyDetail.verlauf (27) mixed cells + prose, replaced with commas/periods/parens per CLAUDE.md (colons only in headings and inside fake vote titles in box art, where the 1-char swap preserves ASCII alignment). Verified: grep -rP for U+2014|U+2013 across apps/**/*.mock.md returns zero, including the new speeches.mock.md.
- hygiene 2026-07-12: item 6 done, no source code touched, no commit. Lockfile: npm wins on evidence (see resolved Open question); deleted pnpm-lock.yaml + pnpm-workspace.yaml and the stale pnpm remnants inside node_modules (.modules.yaml, .pnpm dir). Full `npm install` exit 0; it pruned the node_modules/@machtblick symlinks for the two removed workspaces but left their packages-section entries flagged `"extraneous": true` (npm prune would not drop them either, known Arborist behavior for deleted workspace dirs), so the two entries (etl/bundestag-reden, etl/party-lineage-seed) were removed JSON-surgically without touching any locked version, and a second `npm install` reproduced the lockfile byte-for-byte (diff vs HEAD: 105 pure deletions). Workspace globs (db, etl/*, apps/*) are correct as-is: etl/_oneshot and etl/_shared have no package.json (party-lineage-seed's inner package.json was dropped in wave 1, its deps drizzle-orm + @machtblick/db resolve hoisted at root, verified present). No packages/ dir exists despite the CLAUDE.md layout mention. Untracked artifacts: all four confirmed regenerated by `npm run build` (og-image.png via scripts/generate-og-images.mjs stage 2 of the build script; sitemap.xml, votes.xml, speeches-people.json written at vite.config.ts load, lines 103/258 and writeSpeechesStatic at 221) and deployer.md's pipeline is exactly `npm run build && wrangler pages deploy`, so all four were git rm --cached'd (still on disk) and added to .gitignore next to the existing public/ block. Sqlite cruft: db/{bundestag.sqlite,data.sqlite,dev.sqlite,bundestag.db,dev.db} all verified 0 bytes and untracked, deleted; db/machtblick.sqlite (271MB) untouched. Gates: root `npx tsc --noEmit` exit 0, check-theme-contract.mjs passes. git status delta from this work: M .gitignore, M package-lock.json, D pnpm-lock.yaml, D pnpm-workspace.yaml, four staged D (cached) under apps/bundestag/public.
- lead 2026-07-12: plan created at HEAD 393aa7d. All findings re-verified present on fresh HEAD (6 term-21 SQL literals, 4 CURRENT_TERM redeclares, rmSync at vite.config:187, dead views/fns confirmed zero importers, 8 honorifics files, N+1 loop in seats.ts). Baseline build started. Dispatching wave 1: backend, frontend, ios, plumber in parallel with disjoint file sets. Wave 2 (vite.config restructure, repo hygiene, handzeichen materialization, mocks) follows after wave-1 gates pass.
- plumber-wave2 2026-07-12: item 8 done. Part A: new materializeHandzeichenTallies() in db/materialize-derived-data.ts fills vote_party_summaries.members/yes/no/abstain/absent for every non-namentlich vote's rows (members = party seat count, yes/no/abstain = seats where position matches else 0, absent = 0, mixed keeps zeros with members set); no schema change needed, the columns existed and were NULL on all 1354 handzeichen rows (hammelsprung has 0 summary rows). Seat inference ported as-is (last 20 term-21 namentlich by date desc, first non-null members per party, stop at 6); before porting, a scratch harness proved all three prior read-path inferences (seats.ts getSeatsByParty, vite-data latestSeatsByParty, listVotes/leanVotes inline filtered-rows variant) produce the identical map on current data, so one stored value is safe. DB backup at /tmp/mb-sqlite-backup.sqlite; materialize run against db/machtblick.sqlite (1354 rows filled, 0 NULLs remain); idempotency proven by identical sqlite3 .dump sha256 before/after a second full run. Read paths simplified to plain stored-column reads with per-branch reduce() sums for vote totals: src/server/votes.ts (inline seat inference + synthesis deleted, branches unified), voteDetail.ts, antraege.ts, vite-data/votes.ts (leanVotes + fullVote), vite-data/antraege.ts (positionsByVote synthesis -> stored-tally sums); seats.ts getSeatsByParty deleted (zero callers remain), getChamberSize stays (voteDetail + partyDetail). Consumer audit found ONE NULL-tally-sensitive reader outside my file set: partyDetail.ts used `yes != null` as a namentlich detector on an unfiltered term-21 summary list, so the fill would have silently pulled handzeichen rows into party cohesion/attendance averages; fixed with an explicit vote_type='namentlich' where-clause (behavior-identical to baseline, one line, flagged here since partyDetail.ts was not in my nominal file set). All other consumers verified safe (members/memberDetail/vite-data members only look up namentlich vote ids via vote_members; parties/leanParties filter namentlich; polarity apply.mjs seat lookup is namentlich-scoped; write.mjs upsert only touches position so re-ingests + polarity flips self-heal at the next db:materialize, which both refresh drivers already mandate after vote ingests). Dead-but-kept plumbing for lead: vite-data latestSeatsByParty/VoteBuildData.seats/fullAntrag's seats param are now unused internally but build/jsonEndpoints.ts (item-3 ownership, off-limits to me) still passes voteData.seats; droppable on both sides in a follow-up. Part B decisions: cleanAbstract (2 identical copies, tag-stripping = CLEANING) deleted from src/server/antraege.ts + vite-data/antraege.ts, both now read the already-materialized antraege.abstract_plain (materializeAntraege writes it with the identical regex chain, freshly re-run); clipSummary (votes.ts) and clipListSummary (antraege.ts) are DISPLAY clipping over locale-overlaid translation text, each single-copy after wave 1, different limits/stripping semantics, kept in the read path unmerged. Verification: tsc --noEmit green in apps/bundestag and root; regenerated all JSON via build/jsonEndpoints writeJsonEndpoints against the filled DB; diff -rq vs /tmp/mb-baseline-artifacts BYTE-IDENTICAL for public/{api,votes,motions,members,en} (en includes en/parties) plus speeches-people.json; top-level public/parties has no baseline capture but the en variant of the same fullParty code path matched. plumber.md updated: "Handzeichen party tallies are materialized (plan 109)" section + amended the stale "no headcounts" claim under Tables. No commit.
- backend-wave2 2026-07-12: item 3 done. vite.config.ts is 33 lines of pure config assembly; the four sqlite-backed generators moved to apps/bundestag/build/: prerenderPaths.ts, sitemap.ts (sitemapEntries internal, exports no-arg writeSitemap), jsonEndpoints.ts, votesFeed.ts, plus latestVoteDate.ts (feeds the __DATA_LAST_MODIFIED__ define, needed on dev too, one cheap readonly query) and shared.ts (openDb, SITE_URL, publishableAntragIds, and PARTY_SLUGS consolidating the 3 identical local slugMaps). Generator bodies verbatim otherwise; CURRENT_TERM now imported from src/server/term.ts, killing vite.config's own literal-21 redeclare that item 1's grep scope missed. Gating: defineConfig(({ command }) => ...), writers run only when command === 'build'; tanstackStart pages gets prerenderPaths() on build and [] on serve (verified in @tanstack/start-plugin-core dist that pages is consumed only by prerender.js/post-build.js/build-sitemap.js, all post-build phase), so dev opens the sqlite exactly once (latestVoteDate) instead of 5+ times and does zero fs work. rmSync+mkdirSync teardown replaced by idempotent writeDir: mkdirSync recursive, write files in place, then prune only entries not written this pass (root/en parties dirs keep their original never-pruned semantics; obsolete public/antraege + public/en/antraege rmSync kept, build-only, force+recursive with no mkdir pair so no race). Dev-boot proof: vite dev --port 5199 ready in 1.7s, mtime+size fingerprint of all 1.3M public/ files identical before/after boot, zero files touched, no ENOTEMPTY. Build proof: npx vite build exit 0, no ENOTEMPTY; regenerated public/{api,en,votes,motions,members,sitemap.xml,votes.xml,speeches-people.json} byte-identical to /tmp/mb-baseline-artifacts (diff -rq clean on every path), and full generated set incl. parties + all speeches-search shards byte-identical to a pre-change snapshot; antraege dirs correctly absent. tsc --noEmit green (build/ enters the program via vite.config.ts imports, same as vite-data). No commit.
- lead 2026-07-12: post-wave-2 dormant-path sweep (designer flags + item-8 follow-up), done inline: deleted StanceText.tsx (component never rendered; Stance type inlined into Reader), removed SpeechEntry's unreachable turn/nested branches and mode prop (only call site RedenSearch used mode="card"), removed Reader's dead ReaderSummaryItem variant (both call sites only construct speech items; ReaderItem is now the flat speech shape, kind discriminator dropped at all 3 sites), removed dead seats plumbing (fullAntrag seats param, VoteBuildData.seats, latestSeatsByParty in vite-data/votes.ts) left over from item 8, pruned newly orphaned copy keys (zwischenfrage, partySummaryAria, partySummaryNotice) and orphaned imports. tsc green, theme + more-ui contracts green.
- tester 2026-07-12: full 10-flow regression sweep against http://127.0.0.1:5174 (local vite dev serving the changed tree), Chromium headless, desktop 1440x900 + mobile 390x844, 12 Playwright cases, final run ALL GREEN, zero console errors and zero console warnings on every flow. (1) /votes/ feed PASS: cards with verdict stamp, serif summary, party donut row; DonutSlices hover dims siblings to opacity 0.3 and restores. (2) namentlich detail (unifil) PASS: hemicycle >200 dots, party summaries, Reden tab debate; all 5 expanded bubbles grew 2.5-4k chars (full texts load inline). (2b) Reader overlay from debate list PASS: opened via "Ganze Rede lesen" (path only reachable when a speech id is absent from the texts map, forced by fulfilling speeches-search shards with {}; identical logic pre/post cleanup per diff), speaker header + party badge + body, prev/next walk Wortmeldung 1->2->3 and back (turns 1+2 are both Jürgen Hardt, a split speech, so counter not name is the signal), X and Escape both close. (3) handzeichen detail pp21-86-0 PASS: materialized tallies exact vs DB (CDU/CSU Nein 208, SPD Nein 120, Grüne Ja 85, AfD Enth 150, Linke Enth 64, sum 627). (4) /speeches/ "Klima" PASS: mark-highlighted cards, Reader with highlights, X + Escape. (5) member merz-friedrich PASS: stats, voting record, speeches tab, MemberDebateDialog opens/closes; motions tab absent because Merz has 0 authored motions (count-gated, not a regression). (6) /parties/spd/ PASS: Geschlossenheit + Anwesenheit plausible pcts, recharts seat-history renders. (7) /motions/ + 321943 PASS: clean abstract, no <br>/markdown/undefined artifacts. (8) DE->EN toggle PASS: /en/votes/ Accepted/Rejected labels, EN detail donut "Yes ...", no undefined anywhere. (9) theme toggle PASS: data-theme dark/light flips, body background changes. (10) mobile filter sheet PASS: FAB opens sheet, Abgelehnt applies (?result=abgelehnt, top cards all Abgelehnt). (11) mobile Reader PASS: bottom sheet at y>=64 with drag handle. Regressions found: none. Notes for lead: routes are /speeches/ and /motions/ (dispatch said /reden/ and /antraege/, those 404 as expected post-cleanup); one dev-server crash mid-run under 5 parallel workers (ERR_CONNECTION_REFUSED, environment not app; final green run used 3 workers); no default namentlich feed filter on this tree (FAB starts at bare "Filter").
