# Release Automation

## Goal

Eliminate manual App Store submission: a manually dispatched GitHub Action that submits an existing TestFlight build for full Apple review. Manual trigger only (`workflow_dispatch`), no push or schedule triggers. The operator releases the approved version by hand (`automatic_release: false`).

## Status

Done.

## Scope

- New Fastfile lane `release` submitting an existing TestFlight build via `upload_to_app_store`.
- New workflow `.github/workflows/ios-appstore-release.yml`.
- No changes to existing workflows, no triggers fired, nothing committed.

## Contracts

- Marketing version is centralized in `apps/ios/Config/Version.xcconfig` (enforced by `apps/ios/scripts/check-release-version.mjs`); the lane refuses to submit unless the dispatched `version` input matches it. CI never edits the version.
- `build_number` input is optional; empty means the latest TestFlight build for that version.
- Metadata and screenshots are owned by the `app_store_assets` lane and its workflow, so the release lane runs with `skip_metadata` and `skip_screenshots`.
- `ITSAppUsesNonExemptEncryption` is declared in `apps/ios/src/Info.plist`, so no `submission_information` block is needed.

## Log

### backend

- Studied `beta_local` and `app_store_assets` lanes, `ios-testflight.yml`, `ios-app-store-assets.yml`, and `check-release-version.mjs`; found the marketing version lives in `Version.xcconfig` (currently 1.2), not the pbxproj.
- Added the `release` lane: verifies the `version` option against `APP_VERSION`, resolves the build number (input or latest TestFlight for the version), exports `RELEASE_BUILD_NUMBER` to `GITHUB_ENV` in CI, then `upload_to_app_store` with `skip_binary_upload`, `submit_for_review: true`, `automatic_release: false`, `precheck_include_in_app_purchases: false`.
- Added `ios-appstore-release.yml`: `workflow_dispatch` only (required `version`, optional `build_number`), owner-gated like the other iOS workflows, `ios-appstore-release` concurrency group without cancel-in-progress, macos-15 runner, the shared release-version check, fastlane 2.237.0, the three `APP_STORE_CONNECT_API_*` secrets, and a summary step echoing version and submitted build number.
- Verified the workflow parses with `yaml.safe_load`, the Fastfile passes `ruby -c`, `check-release-version.mjs` still passes, and secret names match `ios-testflight.yml`.

### ios-polish (keyboard tap-through fix)

- Operator bug: with a search field focused, tapping content dismissed the keyboard but the same tap also navigated (e.g. member row). Desired: first tap only dismisses, second tap navigates.
- Inventory of text inputs: `.searchable` on `Features/Votes/UI/VotesFeedView.swift` and `Features/Members/UI/MembersGridView.swift`; custom `Core/UI/SearchField.swift` (plain `TextField`, no `@FocusState`) used by `Features/Members/UI/MemberVotesPanel.swift`, `Features/Members/UI/MemberSpeechesPanel.swift`, `Features/Speeches/UI/DebatePanel.swift`. No other TextFields.
- Root cause: `Core/UI/KeyboardDismisser.swift` (window-level `UITapGestureRecognizer` installed from `RootTabView`) had `cancelsTouchesInView = false`, deliberately letting the dismissing tap also reach content.
- Mechanism chosen: fix the existing central dismisser instead of five per-screen SwiftUI overlays. Tracks keyboard visibility via `keyboardWillShow`/`keyboardWillHide`; the tap recognizer only receives touches while the keyboard is up (`shouldReceive` returns `keyboardVisible`), keeps default `cancelsTouchesInView = true`, and adds `shouldBeRequiredToFailBy` returning `keyboardVisible` so SwiftUI's internal row/link recognizers must wait for the dismiss tap to fail, swallowing the first tap. Keyboard down means the recognizer never participates, so normal taps are untouched. Scroll pans fail the tap immediately, so `.scrollDismissesKeyboard(.interactively)` behavior is unchanged. Taps on text fields and UIControls (search-bar cancel, tab bar) still bypass, as before.
- Files touched: `apps/ios/src/Core/UI/KeyboardDismisser.swift` only. All four `apps/ios/scripts/*.mjs` contract checks green (`check-release-version.mjs` from repo root). Not committed; CI compiles on push.

### ios-polish (double x on search)

- Operator bug: active search (screenshot: "hashs", "Keine Treffer") showed two x buttons, the system in-field clear (`UISearchTextField`'s `clearButtonMode` circular filled xmark) plus the outer dismiss X from iOS 26 `.searchToolbarBehavior(.minimize)`. Wanted exactly one: keep the outer dismiss X, drop the in-field clear, and have dismissal also reset the query so reopening starts empty.
- Screen identified as `Features/Votes/UI/VotesFeedView.swift` (`copy.noResults` plus `.searchable` with minimize behavior); `Features/Members/UI/MembersGridView.swift` shares the same `.searchable` pattern. The custom `Core/UI/SearchField.swift` panels have no clear button or dismiss X, so they are untouched.
- New shared component `apps/ios/src/Core/UI/SearchDismiss.swift`: a `SearchClearButtonRemover` singleton (selector-based observer on `UITextField.textDidBeginEditingNotification`, same pattern as `KeyboardDismisser`) sets `clearButtonMode = .never` on any `UISearchTextField` the moment it begins editing, which kills the in-field clear without touching the custom `SearchField` TextFields; a `ViewModifier` reading `@Environment(\.isSearching)` sets the bound query to `""` whenever search deactivates, exposed as `.clearsQueryOnSearchDismiss($store.search)` and applied directly inside `.searchable` on both VotesFeedView and MembersGridView.
- Files touched: new `SearchDismiss.swift`, one modifier line each in `VotesFeedView.swift` and `MembersGridView.swift`. No new Copy keys, no pbxproj edits (filesystem-synchronized groups pick up the new file). All four `apps/ios/scripts/*.mjs` contract checks green (`check-release-version.mjs` from repo root). Not committed.

### plumber (HTML-entity pollution in stored text)

- Operator bug: vote `2026-07-10-1019-ablehnung-des-antrags-der-linken-39-krankenversicherte-entlasten-nicht` rendered a literal `&#39;` in its official title (iOS + web). Measured pollution: `votes.title` 1, `vote_documents.title` 1, `vote_translations.title` 1 (EN), `vote_polarity_decisions.original_title` 1, `antraege.abstract` 68 (`&#39;`/`&quot;`/`&ndash;` and friends, my earlier LIKE-based count of 38 undercounted), `antraege.abstract_plain` same 68 (derived). All other swept columns (speeches, summaries, party positions, all translation tables, agenda items) were clean.
- Root cause: every scrape decoded entities with hand-rolled partial replace chains. `text()` in `etl/bundestag/votes/import-namentlich.ts` handled only `&nbsp;/&amp;/&quot;`, so the bundestag.de list HTML's `&#39;` survived into `votes.title` (and its `39` leaked into the vote slug via `slugify`). Same class of gap in `detailBallots.ts`, `handzeichen/segment.mjs`, `backfillAgendaItem.ts`. DIP JSON abstracts arrive HTML-escaped and `etl/dip/buildAntraege.ts` never decoded them; `plainText()` in `db/materialize-derived-data.ts` strips tags but not entities, so `abstract_plain` inherited the pollution.
- Fix at entry: new shared decoder `etl/_shared/entities.mjs` (+ `entities.d.mts`), `decodeHtmlEntities` = single-pass named + numeric (dec/hex) entity decode; `&shy;`/`&#173;` drop, `&nbsp;`/`&#160;` to plain space, unknown entities left alone (no double-decode: `&amp;amp;` becomes `&amp;`). Wired into `import-namentlich.ts` `text()`, `detailBallots.ts` `htmlText()`, `handzeichen/segment.mjs` `extractText()`, `votes/backfillAgendaItem.ts` `textFromXml()`, and `etl/dip/buildAntraege.ts` (title + abstract). fast-xml-parser paths already decode via default `processEntities`.
- Repair: new idempotent sweep `db/decode-html-entities.ts` (`npm run db:decode-entities`), sqlite backed up to scratchpad first. Decoded: votes.title 1, vote_documents.title 1, antraege.abstract 68, vote_translations.title 1, vote_polarity_decisions.original_title 1; second run decodes 0 (converged). `db:materialize` re-derived the 68 `abstract_plain` rows; post-run entity counts are 0 everywhere. 15 speeches rows match the `&...;` shape but are genuine prose (`& Anwendung“.`), correctly untouched.
- Decision: the vote id/slug keeps the leaked `-39-` (`...der-linken-39-krankenversicherte...`). The URL is public and stable; changing ids breaks links and caches. Only the display fields were repaired.
- Derived pipeline for that vote checked: `clean_title` ("Keine höheren Kosten oder Leistungskürzungen für Krankenversicherte") and `vote_polarity_decisions.rewritten_title` were already clean; the polluted EN translation title was decoded in place and will also self-heal on the next `etl:translations` (source hash changed).
- Regenerated public endpoints (sitemap + votes feed + JSON endpoints, same generators the vite build runs). Isolated fix-only delta (regenerated from backup DB vs repaired DB, same code): 1449 files. 1260 member JSONs (630 x de/en: every sitting member's vote history carries the repaired namentlich title), 147 motion JSONs (68 decoded abstracts x locales plus list spillover), 26 vote JSONs, 12 party JSONs, 2 api/votes.json, 2 speeches-meta.json. `votes.xml`/`sitemap.xml` unchanged (feed uses clean_title). Zero entity patterns remain in public JSON.
- Guard: decode runs at ingest (see above) plus belt-and-braces `db:decode-entities` chained into `etl/bundestag/handzeichen/refresh.mjs` (after write, before proposers) and added as step 8 of the auto-refresh source list in `prompts/auto-refresh.md` (later steps renumbered). Quirk documented in `.claude/agents/plumber.md` under shared modules.
- `tsc --noEmit` green at root and in `apps/bundestag`. Nothing committed; lead ships.

### ios-polish (vote detail design drift: tab strip and notice boxes)

- Operator drift report: the vote detail tab strip (Ergebnis/Details/Reden) and the surface notice boxes diverged from the web reference.
- Tab strip: iOS used the native `Picker(.segmented)` in two places, `Features/Votes/UI/VoteDetailView.swift` `picker()` and `Features/Members/UI/MemberDetailTabs.swift` (member detail Votes/Speeches, mounted from `MemberDetailContent.swift`). Web reference (`views/voteDetail/VoteDetailTabs.tsx`, `views/memberDetail/MemberDetailTabs.tsx`): `rounded-m border border-fg/15 bg-surface p-xs`, active segment `bg-background font-semibold shadow-[0_1px_2px_rgba(10,10,10,0.08)]` with inner radius `calc(radius-m - spacing-xs)`, inactive `opacity-l`. New shared `apps/ios/src/Core/UI/SegmentedTabs.swift` mirrors it exactly: container `RoundedRectangle` Radius.m (14) with `ThemeColor.border` (fg @ Opacity.s = 0.15, the existing fg/15 convention) at Stroke.s, surface fill via `.background(_:in:)` so the active-segment shadow is not clipped, Spacing.xs inner padding and gap; active segment `RoundedRectangle` at Radius.m - Spacing.xs = 10, background fill, semibold, shadow 0x0A0A0A @ 0.08 radius 1 y 1; inactive regular at Opacity.l. Both call sites now use it. `MembersFilterSheet.swift`'s segmented sort-direction picker stays native: it lives in a native Form sheet, not the detail tab-strip pattern.
- Notice boxes: web renders all three (`officialDataNotice` in ResultTab.tsx, `aiSummaryNotice` in DetailTab.tsx, inverted/petition notices in VoteDetail.tsx) as `bg-surface p-m text-s`, sharp, borderless. Verified line by line that the three iOS counterparts in VoteDetailView.swift (lines 144, 167, 187) already render sharp: plain `.background(ThemeColor.surface)` on the padded Text, no clipShape/cornerRadius in the chain, no global appearance override, and the only rounding sites in `apps/ios/src` are unrelated (MemberBallotBadge, MemberCardView, StampView, SearchField). No change needed there. MotionDetailView and PartyDetailView have neither pattern.
- `check-more-ui-contract.mjs` asserts nothing about these views' tab or notice styling, so no contract fragment changes. No new Copy keys, no pbxproj edits (filesystem-synchronized groups). All four `apps/ios/scripts/*.mjs` contract checks green (`check-release-version.mjs` from repo root). Not committed.

### frontend (web mirror: rounded notice boxes + inline notice links)

- Operator decision applied to web: gray `bg-surface` notice boxes get `rounded-m` (consistent with cards/tabs/Reader), trailing "click here" links become inline links on the meaningful phrase.
- Rounded: all four unrounded `bg-surface p-m text-s` notice boxes in `apps/bundestag/src/views`: `voteDetail/ResultTab.tsx` (official data), `voteDetail/DetailTab.tsx` (AI summary), `voteDetail/VoteDetail.tsx` (inverted-vote and petition-bundle notices). Full-bleed surfaces (nav, sheets, day bands) untouched; sweep confirmed no motion/member/party or methodology equivalents exist (Methodik.tsx has no surface boxes).
- Inline link: ResultTab now renders DE "Alle Zahlen auf dieser Seite basieren auf den [offiziellen Daten des Deutschen Bundestages]." / EN "All figures on this page are based on the [official data of the German Bundestag]." with the phrase as the underlined link to `vote.sourceUrl`. Copy keys `officialDataNotice` + `officialDataLink` replaced by `officialDataNoticePrefix` + `officialDataNoticeLink` in `src/lib/copy/de.ts`/`en.ts` (old keys had no other web consumers; iOS reads its own `Localizable.xcstrings`, untouched). Grep for `↗` across views found no other prose-notice trailing links; DetailTab's Drucksache PDF link is already inline on the meaningful label.
- Contracts: `check-more-ui-contract.mjs` only asserts `aboutData`/`sourceCode` web copy fragments, `check-theme-contract.mjs` only `hover:bg-surface`; neither touched the changed fragments, so no contract edits needed. `tsc --noEmit`, theme contract, and more-ui contract all green. Playwright screenshot of the Krankenversicherte vote detail on :5174 verified rounded inverted-vote and official-data notices plus the inline underlined phrase link. Not committed.

### ios-polish (iOS mirror: rounded notice boxes + inline notice links)

- Operator decision applied to iOS: gray `ThemeColor.surface` notice boxes get RoundedRectangle at `ThemeTokens.Radius.m` via `.background(_:in:)` (the `SegmentedTabs.swift` container pattern), trailing "click here" links become inline links on the meaningful phrase.
- Rounded: all three notice boxes in `apps/ios/src/Features/Votes/UI/VoteDetailView.swift` (official-data notice in `ergebnis`, AI-summary notice in `details`, `noticeBox` used by inverted-vote and petition-bundle notices). Sweep of `apps/ios/src` for `ThemeColor.surface` confirmed those are the only surface notice boxes; the rest are avatars/circles (`SpeakerAvatar`, `MemberAvatar`, `AvatarPile`, `AntragSignatoryStrip`), the already-rounded `SearchField`, `PartySurface` bubbles, and `SegmentedTabs` itself. Motion/Member/Party detail and Settings screens have none; Settings linked paragraphs (`SettingsLinkedParagraph`) already inline their links on the phrase, untouched.
- Inline links: `VoteDetailView` official-data notice now renders DE "Alle Zahlen auf dieser Seite basieren auf den [offiziellen Daten des Deutschen Bundestages]." / EN "All figures on this page are based on [official data from the German Bundestag]." and the AI-summary notice links the phrase DE "Antragstext" / EN "motion text" to the Drucksache PDF, wording otherwise identical, `↗` dropped. Copy restructure: `copy.officialDataNotice` + `copy.officialDataLink` + `copy.fullMotion` replaced by format keys `format.officialDataNotice` and `format.aiSummaryNoticeLinked` carrying the markdown link with a `%@` URL placeholder (`Copy.officialDataNotice(url:)`, `Copy.aiSummaryNoticeLinked(url:)` via the existing `formatted()` helper); plain `copy.aiSummaryNotice` kept for the no-PDF fallback. Rendering via `Text((try? AttributedString(markdown:)) ?? AttributedString(_))` (existing `MarkdownText`/`VoteCardView` pattern), `.tint(ThemeColor.fg)` kept so links stay visible.
- Grep for `↗` and markdown-link `Text` across `apps/ios/src` found no other prose-notice trailing links; `MotionDetailView`'s `motionPdf` link is a standalone source row, not a sentence notice, untouched.
- Contracts: `check-more-ui-contract.mjs` and `check-settings-content-parity.mjs` assert none of the changed fragments, so no fragment updates were needed. All four `apps/ios/scripts/*.mjs` checks green from repo root (localizations 173/173 with key parity and no unused declarations, more-ui, release-version, settings-parity 232 fields). No pbxproj edits, not committed.

### plumber (husk votes pp21-90-15/16 + handzeichen source_url repair)

- Operator bug: published vote pages `pp21-90-15-gesetzentwurf` and `pp21-90-16-gesetzentwurf` (session 90, 2026-07-10) were husks: title = bare "Gesetzentwurf", NULL topic/subject/initiator/document/agenda_item, synthesized tallies rendering as 630-0-0-0. Root cause in `etl/bundestag/handzeichen/segment.mjs`: each block is clamped at the previous outcome sentence's end, so consecutive readings lose the bill-naming context. Blocks 15/16 were the 2. and 3. Beratung of the Abgeordnetengesetz bill (Gesetzentwurf CDU/CSU+SPD Drs. 21/6330, Beschlussempfehlung 21/6851 Buchstabe a, both unanimously angenommen); the bill name sat in block 14, so the LLM extractor could only emit "Gesetzentwurf" with no Drucksache. Same class, one notch milder: `pp21-87-1` and `pp21-89-2` (third readings, real titles inferred but empty drucksache → NULL document/initiator).
- Extraction fix: `segment.mjs` now emits a `context` field carrying the clamped-away lookback; `extract.mjs` renders it as a Kontext section; `prompts/etl/bundestag/handzeichen-extract-system.md` instructs resolving title/Drucksache from context (positions only from the block) and forbids bare document-type titles. Min-length skip now counts block+context so tiny voting sentences aren't dropped. Verified on re-segmented 21-90: block 15/16 context names the bill and Drs. 21/6330.
- Repair (ids kept, pages are 10 days old and indexed): id pinning added, extracted votes may carry an explicit `id` that `write.mjs` and `db/handzeichen-title-sources.ts` honor over the title-derived slug. One-shot `etl/_oneshot/repair-handzeichen-husk-votes.mjs` (`oneshot:handzeichen:husk-repair`, idempotent, converges to 0 changes) patches the extracted JSONs: 21-90 votes 15/16 pinned to the published ids with title "Abweichung vom Anpassungsverfahren der Abgeordnetenentschädigung für das Jahr 2026" + Drs. 21/6851, 21/6330; 21-87-1 and 21-89-2 get their sibling second-reading drucksachen; repairs stale clean_title and purges the 2 wrong EN "Draft law" translation rows (regenerate via hash mismatch on next etl:translations). Sqlite backed up to scratchpad first. Then the non-LLM chain: write → proposers → procedural → initiator → backfill-initiators → provenance → backfill-agenda → reading-pairs → materialize. All four votes now carry real titles, canonical documents, initiators (CDU/CSU x2, Bundesregierung x2), agenda items, debate linkage. Descriptions (summary_simplified) backfill on the next scheduled refresh, rows are now eligible. New debate linkage made the 3 repaired vote pages speech-rich with NULL party position summaries (a hard gate failure), so ran `etl:party-positions` targeted at exactly those 3 votes (12 summaries) to hand lead a deployable green gate.
- source_url scope (operator-reported): all 326 handzeichen+hammelsprung rows stored the DIP API endpoint (401 for citizens). `write.mjs` now writes `https://dserver.bundestag.de/btp/<term>/<term><3-digit-session>.pdf` (pattern HEAD-verified 200 for 21/090, 20/100, 12/005); re-running write repaired all 326 rows (0 api URLs remain anywhere). Namentlich untouched and verified clean (bundestag.de links, 0 api).
- Gate: `db/validate-public-votes.ts` adds `husk_generic_titles` (title < 15 chars or bare-generic title/clean_title), `husk_no_provenance` (initiator+document+subject all NULL), `api_source_urls` (`/api/` in source_url). Verified failing pre-repair (2/4/291) and 0 post-repair, exit 0. All-terms sweep beyond the gate scope: only remaining short-title row is WP20 `bt20-2022-06-03-042-1-mindestlohn`, a real legacy-dataset vote (document + summary present), deliberately exempt via the gate's term-21 scope.
- Public delta (sitemap + votes feed + JSON endpoints regenerated): 591 files changed, 0 added, 0 removed. 291 de + 291 en vote JSONs (dserver sourceUrl on every published handzeichen vote; the repaired four also carry titles/documents/initiators/party positions), 3 party JSONs per locale, api/votes.json per locale, votes.xml. sitemap.xml unchanged (slugs stable). Both repaired vote JSONs verified: real title, initiator CDU/CSU, document "Gesetzentwurf der Fraktion der CDU/CSU (Drucksache 21/6851, 21/6330)", dserver sourceUrl, per-party seat tallies, populated debate.
- Quirks documented in `.claude/agents/plumber.md` (segmentation context, id pinning, dserver source_url, husk gate). `tsc --noEmit` green root + app. Nothing committed; lead ships.

### plumber (final anomaly batch: speech linkage audit, antraege gate, vote 1019, summaries, gate hardening)

- **Speech linkage (pp21-90-10 kulturweit): correct, no fix.** Protocol 21090 shows TOP 25 was a joint debate of TOP 25a (Kultur macht stark, Linke), 25b (Staatsziel GG, Linke) and ZP 20 (kulturweit, Grüne, Drs 21/5517 + 21/6987); the vote sits inside that block, so the linked group is the right debate and the party-position summaries honestly disclaim that most speeches address the sibling motions. Spot-checked 89-1/2 (TOP 9 Kindergeld), 89-29/30 (TOP 7 + ZP 9-11 Energie joint), 89-31 (ZP 13), 89-32/33 (TOP 13), 90-0 (TOP 22), 90-2..5 (ZP 28/29), 90-13 (AfD Entschließungsantrag Drs 21/7071 confirmed in the TOP 25 block), 90-14..19 (TOP 24 joint: 24a Entschädigung + 24b Amtsbezüge + ZP 19 GO): all groups match their protocol blocks. Pattern documented in plumber.md (joint debates are correct linkage; verify against the protocol block, not summary wording).
- **Antraege publish gate:** the 5 contentless WP21 antraege (332795, 334134, 334864, 335940, 336303) have no `antrag_descriptions` row, so they were already absent from sitemap/prerender/JSON (all publish paths inner-join descriptions); the hole was a description row with NULL summaries. `publishableAntragIds` (`apps/bundestag/build/shared.ts`) now additionally requires abstract OR summary_simplified OR drucksache_pdf_url (993/993 current rows pass, zero delta), and the gate gained `contentless_published_antraege` with the same condition. They re-publish automatically once DIP delivers documents and a description generates.
- **Vote 1019 truncated title:** upstream list HTML truncates the title mid-quote; full Antrag title recovered from `votes.document` ("Krankenversicherte entlasten, nicht belasten"). `oneshot:vote-1019-title` (idempotent, converges to 0) completes `votes.title`, the matching `vote_documents.title`, and the equally-truncated EN translation title; id/slug untouched. Safe against re-ingest: the namentlich upsert preserves `title` on `inverted = 1` rows. `vote_polarity_decisions.original_title` deliberately keeps the truncated form as the audit record of what upstream said.
- **EN raw-title fallback:** `overlayVote` (`src/server/translations.ts`) never overlaid `title`, so raw German titles surfaced on /en/ (vote JSONs, motions' linkedVotes, party proposals, "Official title" line). `vote_translations.title` has full coverage (2162/2162), so the EN read path now resolves title as translated title → translated cleanTitle → German raw, in: `overlayVote`, `vite-data/votes.ts` (lean + full), `vite-data/antraege.ts` + `src/server/antraege.ts` (linkedVotes), `vite-data/parties.ts` + `src/server/partyDetail.ts` (votes + proposals). Meta heads already used cleanTitle. Not touched: `getVoteSponsors` motion titles in the EN jsonLd `about` block stay German (official Legislation names; translating needs a locale param on that server fn, backend's call). Repaired 90-15/16 still show German on /en/ until `etl:translations` regenerates their purged rows (hash-keyed self-heal).
- **Summaries:** descriptions worker run targeted at the 4 repaired votes; all 4 generated from the correct Antrag PDFs via `pickFromVoteDocument` (90-15/16 ← 21/6330, 87-1 ← 21/6129, 89-2 ← 21/5874), decisions recorded, prompt v6. The 5 personnel/procedural cards (89-17, 89-18, 89-26, 89-27, 89-28) have bare-number documents the PDF pipeline can't resolve; `oneshot:personnel-summaries` fills dry title-derived one-line `summary_simplified` (fill-if-NULL, no `summary_detail`, so the AI-summary notice doesn't render). All 10 target votes now carry an explanatory line.
- **Gate hardening (`db/validate-public-votes.ts`):** new failing checks `truncated_titles` (raw title ending `...`/`…`; the 12 `…`-clamped Sammelübersicht clean_titles are deliberate and exempt), `unbalanced_quote_titles` (unpaired `„“`/`"`, odd quote-like `'` with `d'Ivoire`-style apostrophes exempt), `reading_pair_parity` (same-date same-stem handzeichen twins must share summary+initiator presence; stem helpers extracted to `db/readingPairs.ts` and imported by both the gate and `db/normalize-reading-pairs-and-related-speeches.ts`, no logic change, smoke-run all-zero), `contentless_published_antraege`. Pre-repair the gate failed 1/1/2/0 on exactly the known offenders; final full run all green, exit 0.
- Ops: fresh sqlite backup in scratchpad (`machtblick-backup-20260720-051734.sqlite`) before writes; `db:materialize` + sitemap/votes-feed/JSON endpoints regenerated. Public delta scoped to this batch: 1814 changed, 0 added, 0 removed; 630 de + 630 en member JSONs (1019 namentlich title in every vote history), 353 en/votes + 172 en/motions + 6 en/parties (translated titles now surfacing), 10 de votes (4 generated + 5 minimal summaries + 1019), 6 de parties, api/votes.json both locales, votes.xml (new summaries in entries). sitemap.xml unchanged. `tsc --noEmit` green root + app. Nothing committed; lead ships.
