# Bundestag auto refresh 2026-06-20

## Goal

Refresh Machtblick Bundestag data because upstream Bundestag XML and DIP data changed, then deploy only if data gates, build, visibility, and deployment gates pass.

## Status

Build, tester, and visibility passed. Commit and deploy gates pending.

## Scheduler preflight evidence

Scheduler thread name: `🤖 2026-06-20 Auto`

Scheduler command `scripts/bundestag-auto-refresh-preflight` exited `0` at `20260620T060238Z`.

```text
utc=2026-06-20T06:02:38Z
root=repository root
branch=main
git_status_begin
?? plans/92-logo-variants.md
git_status_end
db_path=db/machtblick.sqlite
db_size_mb=209.7
votes_count=2106
votes_newest_date=2026-06-11
votes_newest_bundestag_id=1008
votes_by_type.hammelsprung=1
votes_by_type.handzeichen=252
votes_by_type.namentlich=1853
speeches_count=27383
speeches_newest_date=2026-06-11
speeches_newest_session=21-83
speeches_without_member=14278
speeches_without_vote_link=24498
antraege_count=919
antraege_newest_updated_at=2026-06-12T17:37:38+02:00
antraege_missing_descriptions=12
vote_antraege_count=159
vote_translations_count=2083
speech_translations_count=2831
antrag_description_translations_count=907
speech_xml_probe.21084=200
speech_xml_probe.21085=404
speech_xml_probe.21086=404
dip_vorgang_updated_start=2026-06-12T17:37:38+02:00
dip_vorgang_num_found=1200
```

Lead reran `scripts/bundestag-auto-refresh-preflight` at `2026-06-20T06:03:39Z` with matching counts and upstream evidence.

## Initial local state

- Branch: `main`
- Local changes before this plan: `?? plans/92-logo-variants.md`
- `plans/92-logo-variants.md` is unrelated logo selection material. It does not affect source ETL, database, build, visibility, or deploy gates for this run.
- New upstream data is present because session XML `21084` returns `200`, local newest speech session is `21-83`, and DIP reports `1200` Vorgänge updated since `2026-06-12T17:37:38+02:00`.

## Baseline counts

```text
votes_count=2106
votes_newest_date=2026-06-11
votes_newest_bundestag_id=1008
speeches_count=27383
speeches_newest_date=2026-06-11
speeches_newest_session=21-83
speeches_without_member=14278
speeches_without_vote_link=24498
antraege_count=919
antraege_newest_updated_at=2026-06-12T17:37:38+02:00
antraege_missing_descriptions=12
vote_antraege_count=159
vote_translations_count=2083
speech_translations_count=2831
antrag_description_translations_count=907
vote_debate_groups_count=213
```

## Shared contracts

- Back up `db/machtblick.sqlite` under `runs/_app-server/db-backups/` before the first write.
- Use existing ETL scripts.
- Do not pass `--force` to derived jobs unless a later log entry records why.
- Run `etl:handzeichen:refresh` as the polarity-aware path and do not run ad hoc normalization after polarity inversion.
- Publish complete slices only. Incomplete DIP rows may remain in SQLite if unrelated slices pass.
- Deploy is allowed by this scheduled prompt only if the refresh is clean, build passes, visibility passes, and data gates for published slices pass.

## Planned commands

Source refresh:

1. `npm run etl:stammdaten`
2. `npm run etl:abgeordnetenwatch`
3. `npm run etl:votes:namentlich`
4. `npm run etl:handzeichen:refresh`
5. `DIP_UPDATED_START=2026-06-12T17:37:38+02:00 npm run etl:dip`
6. `npm run etl:speeches:xml`
7. `npm run etl:affiliations`
8. `npm run db:normalize`

Derived refresh:

1. `npm run etl:titles`
2. `npm run etl:antrag-titles`
3. `npm run etl:descriptions`
4. `npm run etl:antrag-descriptions`
5. `npm run etl:votes:backfill-agenda`
6. `npm run db:materialize`
7. `npm run etl:party-positions`
8. `npm run etl:translations`
9. `npm run etl:antrag-description-translations`
10. `npm run etl:speech-translations`

Conditional source refresh:

- `npm run etl:portraits` if members changed or portraits are missing.
- `npm run etl:terms` if term metadata changed.
- `npm run etl:donations` only if a new donation year or announcement is likely.

Gates:

1. Count votes, speeches, Antraege, vote links, translations, and generated descriptions before and after.
2. Confirm speech XML fetch reaches the newest available session.
3. Confirm new non-procedural votes have clean titles, descriptions, translations, and party positions when eligible.
4. Confirm Antraege metadata, signatories, descriptions, and translations when eligible.
5. Run `npm run build -w @machtblick/bundestag`.
6. Confirm generated static data for new routes exists.
7. Run tester if behavior or routing changed.
8. Run visibility.
9. Use scribe if tracked source changes were made.
10. Use deployer if all gates pass.

## Open questions

- None.

## Log

### lead

- Verified `git status --short` shows only unrelated `plans/92-logo-variants.md` before this plan.
- Verified local database counts directly with SQLite.
- Reran scheduler preflight and confirmed upstream speech XML and DIP activity.
- Backed up `db/machtblick.sqlite` to `runs/_app-server/db-backups/machtblick-2026-06-20T0604-before-auto-refresh.sqlite` with SQLite `.backup`.
- `npm run etl:stammdaten` completed. It reused the existing raw XML and wrote `783` mandate rows.
- `npm run etl:abgeordnetenwatch` completed. It fetched `860` mandates, `630` unique politicians, matched `2` by name, and had `0` unmatched.
- `npm run etl:votes:namentlich` initially used stale term `20` defaults and imported `0` votes.
- Patched `etl/bundestag/votes/import-namentlich.ts` so the default command targets term `21` and abgeordnetenwatch period `161`, and so rows without a Bundestag detail id use the existing generated-ID path.
- Retried `npm run etl:votes:namentlich`. It imported `1` namentliche vote and `630` ballots.
- `npm run etl:handzeichen:refresh` reached session `84`, found `0` new handzeichen blocks, updated `253` existing handzeichen rows, rebuilt agenda and speech links, and generated eligible handzeichen party positions.
- `npm run etl:handzeichen:refresh` failed only at its final `db:validate:votes` gate because the newly imported 2026-06-12 namentliche vote still needs `summary_simplified`. Global derived descriptions run later in this plan, so lead will rerun validation after derived jobs complete.
- `DIP_UPDATED_START=2026-06-12T17:37:38+02:00 npm run etl:dip` completed. It fetched incremental Antrag, Gesetzgebung, Vorgangsposition, and Aktivität data. It ingested `935` current Antraege, scanned `81530` activities, kept `15303`, and resolved `14249` signatories.
- `npm run etl:speeches:xml` completed. It fetched session `84`, confirmed session `85` is not available, and ingested `27689` speeches across `84` sessions. Speech vote linkage is `2981` direct links, and newest speech session is now `21-84`.
- `npm run etl:affiliations` completed. It inserted `1959` affiliation rows for `1871` members.
- `npm run db:normalize` completed and flipped `0` votes.
- Conditional portrait refresh was relevant because current members had missing portrait URLs. `npm run etl:portraits` completed and set `2078` portraits.
- Patched `etl/bundestag/votes/import-namentlich.ts` again so Bundestag list titles truncated with ellipsis can match the detail page by same-date normalized prefix, and so a previous fallback namentlich row with no `bundestag_id` is removed when the canonical Bundestag detail id appears.
- Verified `npx tsc --noEmit --allowImportingTsExtensions --moduleResolution bundler --module esnext --target es2022 etl/bundestag/votes/import-namentlich.ts` passes.
- Reran `npm run etl:votes:namentlich` after the matching fix. It imported `1` canonical namentliche vote and `630` ballots.
- Verified current-term namentliche votes with `bundestag_id IS NULL` is `0`.
- Reran `npm run etl:dip:process` so DIP vote links use the canonical namentlich id. It retained `159` total `vote_antraege` links.
- `npm run etl:titles` completed. It saw `3` missing titles, wrote `1`, and skipped `2` low-confidence rows.
- `npm run etl:antrag-titles` completed. It wrote `1` title.
- `npm run etl:descriptions -- --vote 2026-06-12-1009-gesetzentwurf-zur-verhinderung-missbrauchlicher-anerkennungen-der-vaterschaft` completed with `1/1` generated and `0` LLM failures.
- `npm run etl:descriptions` completed the remaining default stale queue with `19/19` skipped because no PDF was available and `0` LLM failures.
- `npm run etl:antrag-descriptions` completed `15/16` eligible rows. The failed row is DIP `336194`, Drucksache `21/6524`, because `https://dserver.bundestag.de/btd/21/065/2106524.pdf` returns `404`.
- `npm run etl:votes:backfill-agenda` completed. It processed `310` votes across `84` sessions, resolved `294`, and left `16` unresolved rows.
- `npm run db:materialize` completed. It materialized `776` agenda items, `935` Antrag abstracts, `684` speech debate groups, `4186` speech vote links, and `214` vote debate groups.
- `npm run etl:party-positions` completed. It generated `5/5` eligible party positions for the new namentlich vote.
- `npm run etl:translations` completed `236/236` eligible vote translations.
- `npm run etl:antrag-description-translations` completed `38/38` eligible Antrag description translations.
- `npm run etl:speech-translations` completed `96/96` eligible speech translations.
- Reran `npm run db:validate:votes`. It passed with `public_votes=279`, `missing_clean_title=0`, `bad_namentlich_source_url=0`, `missing_vote_summaries_with_pdf=0`, `missing_speech_rich_party_summaries=0`, and `missing_speech_rich_party_summary_votes=0`.
- Final counts:

```text
votes_count=2107
votes_newest_date=2026-06-12
votes_newest_bundestag_id=1009
speeches_count=27689
speeches_newest_date=2026-06-12
speeches_newest_session=21-84
speeches_without_member=14426
speeches_without_vote_link=24708
antraege_count=935
antraege_newest_updated_at=2026-06-19T15:35:01+02:00
antraege_missing_descriptions=13
vote_antraege_count=159
vote_translations_count=2084
speech_translations_count=2927
antrag_description_translations_count=922
vote_debate_groups_count=214
speech_vote_links_count=4186
vote_descriptions_count=264
antrag_descriptions_count=922
```

- New vote gate passed for `2026-06-12-1009-gesetzentwurf-zur-verhinderung-missbrauchlicher-anerkennungen-der-vaterschaft`: clean title present, description present, English translation present, `5` party positions present, and `1` debate group present.
- Recent Antrag gate passed for complete slices: described Antraege have translations. DIP `336194` remains incomplete because its PDF is unavailable, and generated output does not publish it.
- `npm run build -w @machtblick/bundestag` passed. It transformed `2900` client modules, `282` SSR modules, and prerendered `8524` pages.
- Static route checks passed for the new vote: German HTML, English HTML, and JSON exist under `apps/bundestag/dist/client`.
- Static route checks passed for complete motion `336273`: German HTML, English HTML, and JSON exist.
- Static route checks passed for incomplete motion `336194`: no generated output exists.
- `apps/bundestag/public/sitemap.xml` was updated by the build to include the new vote and complete new motion routes. It does not include incomplete motion `336194`.
- Tester passed against `http://127.0.0.1:4173` serving `apps/bundestag/dist/client`. German and English new vote pages rendered without console errors, German and English motion `336273` rendered without console errors, and motion `336194` returned `404` in both languages.

### plumber

- Reviewed the data refresh plan and flagged the namentlich current-term null `bundestag_id` check, the handzeichen polarity ordering hazard, and the need to verify session `84` speech XML.
- Confirmed session `84` handzeichen extraction having zero blocks is plausible when raw and extracted files exist and the final fetch reaches protocol `21-84`.

### visibility

- Skipped rebuild because lead recorded `npm run build -w @machtblick/bundestag` passed and the task supplied `apps/bundestag/dist/client` as built output.
- `rg -n "2026-06-12-1009-gesetzentwurf-zur-verhinderung-missbrauchlicher-anerkennungen-der-vaterschaft|336273" apps/bundestag/public/sitemap.xml apps/bundestag/dist/client/sitemap.xml` found the new vote at lines `571` and `572` in both sitemaps, and motion `336273` at tracked sitemap lines `1494` and `2416` with matching built sitemap entries.
- `find apps/bundestag/dist/client -path "*2026-06-12-1009-gesetzentwurf-zur-verhinderung-missbrauchlicher-anerkennungen-der-vaterschaft*" -o -path "*336273*" -o -path "*336194*"` found German and English HTML for the new vote and motion `336273`, JSON for the new vote, and German plus English JSON for motion `336273`.
- `rg -n "336194" apps/bundestag/public/sitemap.xml apps/bundestag/dist/client || true` returned no matches, so incomplete motion `336194` is not in the tracked sitemap or generated client output.
- Parsed `votes/index.html`, `en/votes/index.html`, the German and English new vote pages, and the German and English `336273` motion pages. Each sampled page has a page-specific title and meta description, absolute canonical, `de`, `en`, and `x-default` alternates, Open Graph and X title, description, image, image alt, and indexable robots meta with `max-image-preview:large`.
- Reciprocal metadata validation reported `metadata link validation failures=0`; sampled canonical and hreflang targets exist, point to canonical URLs in the matching language, and reciprocate the language cluster.
- Full sitemap validation reported `sitemapUrls=3940`, `lastmodCount=0`, and `badCount=0`; every sitemap URL maps to generated `index.html`, has a matching canonical, has no query or legacy route, and is indexable.
- JSON alternate links on sampled detail pages point to real generated JSON files. The new English vote page points to the shared vote JSON at `/votes/2026-06-12-1009-gesetzentwurf-zur-verhinderung-missbrauchlicher-anerkennungen-der-vaterschaft.json`.
- JSON-LD parsed on sampled pages as site-level `Organization`, `WebSite`, and `DataCatalog` with dataset distributions for vote, member, and party JSON endpoints.
- Static discovery passed: `robots.txt` declares `Sitemap: https://machtblick.de/sitemap.xml` and the intended `Content-Signal`; `llms.txt` documents route families, JSON endpoints, sources, and AI access policy; `.well-known/api-catalog` parses and all service docs plus service-desc links exist; `_headers` exposes `/.well-known/api-catalog` and `/llms.txt`.
- Asset checks passed: `og-image.png` is `1200x630`, manifest icons exist at `192x192` and `512x512`, `apple-touch-icon.png` is `180x180`, `mstile-150x150.png` is `150x150`, and `favicon.ico` contains `16x16`, `32x32`, and `48x48`.
- Checked named crawler rules against current primary docs for OpenAI, Anthropic, Perplexity, Google, Apple, and Common Crawl. The generated robots policy preserves search and user-requested AI retrieval while disallowing listed training crawlers.
- Nonblocking note: motion `336273` meta and social descriptions preserve markdown emphasis markers from generated summaries. They remain specific and complete, so this is not a deploy blocker.
- Visibility gate passed. No deploy-blocking issues found. Changed only `plans/104-auto-refresh-2026-06-20.md`.
