# Bundestag auto refresh 2026-06-27

## Goal

Refresh Bundestag public data when upstream changed, verify complete publishable slices, then commit and deploy only if data, build, visibility, and deployment gates pass.

## Status

Verified. Ready for commit and deploy.

## Scheduler preflight evidence

- Preflight time: `2026-06-27T06:26:31Z`
- Script: `scripts/bundestag-auto-refresh-preflight`
- Exit code: `0`
- Branch: `main`
- Git status at preflight: `?? plans/92-logo-variants.md`
- Database: `db/machtblick.sqlite`, `209.7 MB`
- Votes: `2107`, newest date `2026-06-12`, newest Bundestag id `1009`
- Vote types: `hammelsprung=1`, `handzeichen=252`, `namentlich=1854`
- Speeches: `27689`, newest date `2026-06-12`, newest session `21-84`
- Speeches without member: `14426`
- Speeches without vote link: `24708`
- Antraege: `935`, newest update `2026-06-19T15:35:01+02:00`
- Antraege missing descriptions: `13`
- Vote Antraege: `159`
- Vote translations: `2084`
- Speech translations: `2927`
- Antrag description translations: `922`
- Speech XML probes: `21085=200`, `21086=200`, `21087=404`
- DIP Vorgang updated start: `2026-06-19T15:35:01+02:00`
- DIP Vorgang num found: `1275`

## Lead validation

- `git status --short` currently shows only `?? plans/92-logo-variants.md`.
- `plans/92-logo-variants.md` is unrelated logo exploration and does not touch production assets, code, or data.
- Local DB counts match the preflight for votes, speeches, Antraege, links, descriptions, and translations.
- Upstream work is required because local speeches stop at session `21-84` and upstream XML has `21085` and `21086`.
- DIP also has updates at or after the local `antraege.updated_at` max.

## Shared contracts

- Back up `db/machtblick.sqlite` under `runs/_app-server/db-backups/` before writable data commands.
- Use the existing ETL scripts in the order from the scheduled prompt.
- Do not run ad-hoc DB writes unless they are captured as checked-in scripts.
- `etl:handzeichen:refresh` owns its internal polarity-aware sequence.
- After namentlich ingest, run agenda backfill and materialization before party positions.
- Derived jobs use default stale detection, no `--force`.
- Publish complete slices independently.
- Do not deploy if a gate fails for a slice that would be published.
- Keep unrelated `plans/92-logo-variants.md` uncommitted unless the user explicitly asks otherwise.

## Gates

- Record counts before and after for votes, speeches, Antraege, vote links, translations, and generated descriptions.
- Confirm speech XML fetch reaches newest available session.
- Confirm new non-procedural votes have titles, descriptions, translations, and party positions when eligible.
- Confirm Antraege have metadata, signatories, descriptions, and translations when eligible.
- Run `npm run build -w @machtblick/bundestag`.
- Confirm static data for new routes exists.
- Run `visibility` before deploy.
- Use `scribe` for tracked source changes.
- Use `deployer` only after build and visibility pass.

## Open questions

- Upstream shape change found and fixed: `Drucksachen 21/6417 und 21/5778` needed `und` parsing and direct Antrag PDF lookup.
- Incomplete DIP rows remain unpublished by existing route rules. Described Antraege missing description translations: `0`.

## Counts

### Before

- Votes: `2107`
- Non-procedural votes: `2073`
- Non-procedural votes missing clean titles: `2`
- Non-procedural votes missing summary fields: `1812`
- Vote translations: `2084`
- Non-procedural votes missing translations: `1`
- Vote party summaries: `3866`
- Non-procedural votes without party summaries: `1485`
- Votes with agenda item: `298`
- Vote debate groups: `214`
- Speeches: `27689`
- Speeches newest date: `2026-06-12`
- Speeches newest session: `21-84`
- Speeches with vote link: `2981`
- Speeches without vote link: `24708`
- Speech vote links: `4186`
- Speech translations: `2927`
- Antraege: `935`
- Antraege newest update: `2026-06-19T15:35:01+02:00`
- Vote Antraege: `159`
- Antrag signatories: `14249`
- Antrag descriptions: `922`
- Antraege missing descriptions: `13`
- Antrag description translations: `922`
- Described Antraege missing description translations: `0`

### After

- Votes: `2124`
- Votes newest date: `2026-06-25`
- Votes newest Bundestag id: `1010`
- Non-procedural votes: `2090`
- Term 21 public votes missing clean titles: `0`
- Term 21 public PDF-backed votes missing summaries: `0`
- Vote translations: `2101`
- Term 21 public votes missing English translations: `0`
- Vote party summaries: `3952`
- Vote party summaries with generated position summaries: `931`
- Votes with agenda item: `315`
- Vote debate groups: `216`
- Speeches: `28577`
- Speeches newest date: `2026-06-25`
- Speeches newest session: `21-86`
- Speeches with vote link: `3023`
- Speeches without vote link: `25554`
- Speech vote links: `4227`
- Speech translations: `2969`
- Antraege: `955`
- Antraege newest update: `2026-06-26T15:29:26+02:00`
- Vote Antraege: `164`
- Antrag signatories: `14776`
- Antrag descriptions: `943`
- Antraege missing descriptions: `12`
- Antrag description translations: `943`
- Described Antraege missing description translations: `0`

## Log

### lead

- Created the dated run plan because upstream speech XML and DIP evidence require work.
- Re-ran `scripts/bundestag-auto-refresh-preflight`; it confirmed sessions `21085` and `21086` are available, `21087` is not, and DIP reports `1275` updated Vorgänge since local max.
- Recorded before counts from the local SQLite database.
- Backed up `db/machtblick.sqlite` to `runs/_app-server/db-backups/machtblick-20260627T0629-before-auto-refresh.sqlite`.
- Ran `npm run etl:stammdaten`, `npm run etl:abgeordnetenwatch`, and `npm run etl:votes:namentlich`. The first namentlich attempt failed on a transient socket close while fetching abgeordnetenwatch mandates; retry succeeded and imported Bundestag vote `1010`.
- Ran `npm run etl:handzeichen:refresh`. Initial run failed because local `claude -p` returns API `401 Invalid authentication credentials`, leaving session `21-86` extracted with zero votes and blocking the UNIFIL vote description. Repaired the ETL to fail on extraction batch errors, retry zero-vote extracted files, and use Codex CLI for descriptions, polarity, and titles.
- Generated and inspected a Codex extraction for session `21-86`; rerunning handzeichen refresh inserted `16` handzeichen votes.
- Generated the UNIFIL description, inverted `pp21-86-8-organisierten-wissenschaftsbetrug-unterbinden` through self-no polarity escalation, and reran handzeichen refresh to completion.
- Ran `DIP_UPDATED_START=2026-06-19T15:35:01+02:00 npm run etl:dip`; Antraege increased to `955`, signatories to `14776`, and vote-Antrag links to `164`.
- Ran `npm run etl:speeches:xml`; fetched sessions `85` and `86`, skipped existing `84`, and confirmed latest complete speech XML session `86`.
- Ran `npm run etl:affiliations` and `npm run db:normalize`. The normalization flipped `pp21-86-0-rechte-von-patientinnen-und-patienten-starken` to the substantive rejected result and did not touch the already inverted science-fraud vote.
- Ran derived refreshes: `etl:titles`, `etl:antrag-titles`, `etl:descriptions`, `etl:antrag-descriptions`, `etl:votes:backfill-agenda`, `db:materialize`, `etl:party-positions`, `etl:translations`, `etl:antrag-description-translations`, and `etl:speech-translations`.
- Reran `etl:party-positions -- --vote-type handzeichen` after final speech materialization to generate summaries for `pp21-86-8-organisierten-wissenschaftsbetrug-unterbinden`, then reran vote translations for that vote.
- `npm run db:validate:votes` passes: `public_votes=296`, `missing_clean_title=0`, `missing_vote_summaries_with_pdf=0`, `missing_speech_rich_party_summaries=0`.
- Final preflight confirms speech XML probes `21087=404`, `21088=404`, `21089=404`; local speech newest session is `21-86`.
- `npm run build -w @machtblick/bundestag` passed. Prerender generated the refreshed German and English routes for the new roll-call vote, all `pp21-86-*` handzeichen votes, and new described motion pages.
- Confirmed static route files exist for `/votes/2026-06-25-1010-bundeswehreinsatz-im-libanon-unifil/`, `/en/votes/2026-06-25-1010-bundeswehreinsatz-im-libanon-unifil/`, `/votes/pp21-86-8-organisierten-wissenschaftsbetrug-unterbinden/`, `/en/votes/pp21-86-8-organisierten-wissenschaftsbetrug-unterbinden/`, `/motions/334610/`, and `/en/motions/334610/`.
- `apps/bundestag/public/sitemap.xml` was regenerated by `apps/bundestag/vite.config.ts` and now includes the new vote and motion URLs.
- The plumber note about `0` `pp21-86-%` DB rows was an intermediate state before root reran `etl:handzeichen:refresh`; the final after counts include `16` new session `21-86` handzeichen vote routes.

### plumber

- Investigated the failed `npm run etl:handzeichen:refresh` path. The validator's hard blocker is `2026-06-25-1010-bundeswehreinsatz-im-libanon-unifil`, a new namentlich row with `summary_simplified IS NULL` and a `vote_documents` Bundestag roll-call PDF (`21/86/2`). Its underlying Antrag already exists as `antraege.id=334610`, `drucksache=21/5778`, `initiative_fraktion=Bundesregierung`, `drucksache_pdf_url=https://dserver.bundestag.de/btd/21/057/2105778.pdf`.
- Found why the description picker missed it: `votes.document` says `Drucksachen 21/6417 und 21/5778`, but `etl/bundestag/descriptions/pickAntrag.mjs` only parsed comma-separated Drucksachen and stopped at `21/6417`. Patched the parser to accept `und`, prefer the last Drucksache in Beschlussempfehlung-to-Antrag text, and use matching `antraege` PDFs before DIP fallback.
- Found why session `21/86` did not reach the DB: `etl/bundestag/handzeichen/extract.mjs` caught failed Claude batches, wrote an extracted JSON anyway, and then future runs skipped it because the file existed. Patched extraction to retry existing zero-vote outputs for non-empty block files, fail the process on the first failed batch, preserve the previous output on failure, and include Claude stdout in nonzero-exit errors.
- Verified local Claude CLI auth is currently broken: `claude -p 'Return JSON {"ok": true}' --model sonnet --output-format json` returns API `401 Invalid authentication credentials`. No DB-writing retry was run from plumber while the scheduled root thread was still active.
- Current generated cache state: `etl/bundestag/handzeichen/extracted/21-86.json` now contains 16 extracted vote entries for 9 segmented blocks, but the DB still has `0` rows matching `pp21-86-%`. After the root thread is ready and Claude auth is fixed, retry from the existing ETL sequence with a DB backup first. The relevant changed files are `etl/bundestag/handzeichen/extract.mjs` and `etl/bundestag/descriptions/pickAntrag.mjs`.
- Re-ran read-only `npm run db:validate:votes`; it still fails as expected with `missing_vote_summaries_with_pdf=1` until the UNIFIL description is generated and written. `missing_vote_summaries_total=20` includes that row plus 19 older no-PDF handzeichen rows that are reported but not hard-failing.

### tester

- Served the built app with `npm run start --prefix apps/bundestag -- --host 127.0.0.1` at `http://127.0.0.1:3001` and ran a headless Chromium smoke covering German and English UNIFIL vote pages, science-fraud vote pages, motion `334610`, and German plus English vote listings. All cases passed with `0` console errors and `0` page errors.

### visibility

- Used the existing passed build and inspected generated artifacts under `apps/bundestag/dist/client`.
- Verified German and English vote lists plus `/votes/2026-06-25-1010-bundeswehreinsatz-im-libanon-unifil/`, `/en/votes/2026-06-25-1010-bundeswehreinsatz-im-libanon-unifil/`, `/votes/pp21-86-8-organisierten-wissenschaftsbetrug-unterbinden/`, `/en/votes/pp21-86-8-organisierten-wissenschaftsbetrug-unterbinden/`, `/motions/334610/`, and `/en/motions/334610/`. Titles, descriptions, absolute canonicals, reciprocal `hreflang` clusters, Open Graph fields, X card fields, production robots meta, JSON alternates, and JSON-LD all pass.
- Verified local 200 responses for the sampled canonical HTML pages plus `robots.txt`, `llms.txt`, `.well-known/api-catalog`, and `sitemap.xml` with a temporary static server.
- Verified `sitemap.xml` has `4016` URLs, no `lastmod`, no query or hash variants, no redirect sources, includes the refreshed German and English vote and motion URLs, and has sampled self-canonical consistency.
- Verified `robots.txt`, `llms.txt`, `.well-known/api-catalog`, `_headers`, `site.webmanifest`, favicon assets, manifest icons, and share assets. The default share image is `1200x630`.
- Checked current primary crawler docs for OpenAI, Anthropic, Perplexity, Google, Apple, and Common Crawl. The generated `robots.txt` allows search and user-requested retrieval crawlers and blocks the named training or broad dataset tokens as intended.
- Non-blocking lead attention: motion `334610` meta, Open Graph, and X descriptions include Markdown `**`; the English motion title remains German; sampled HTML contains NUL bytes in serialized router state after the head, which did not block head parsing or local HTTP checks but makes text tools treat the files as binary.
- Visibility gate result: PASS. Blocking issues: none.
