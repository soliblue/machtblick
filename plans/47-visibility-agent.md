# Visibility Agent

Status: Completed

Goal: Add a durable specialist agent for SEO, social sharing, favicon, manifest, structured data, sitemap, robots, and AI assistant discoverability checks, then make deploys expect that check before Cloudflare shipping.

Shared contracts:

- `.claude/agents/*.md` is the source for specialist instructions.
- `.codex/agents/*.toml` is generated by `npm run agents:sync`.
- `visibility` checks production build output and never deploys or commits.
- `deployer` stays focused on Cloudflare deploys and treats `visibility` as a preflight gate.
- This plan covers the agent wiring only. The SEO implementation itself is covered by `plans/46-seo-sharing-ai-review.md`.

Open questions:

- None.

Log:

- lead, 2026-05-18: Started after the user chose one combined visibility agent and asked to commit, push, and deploy.
- lead, 2026-05-18: Added `visibility` source instructions at `.claude/agents/visibility.md`, synced the generated Codex agent, and added it to the team docs.
- lead, 2026-05-18: Updated `deployer` so it expects a `visibility` preflight before Cloudflare deploys while keeping deployer scoped to shipping only.
- lead, 2026-05-18: Ran `npm run agents:sync`, producing `.codex/agents/visibility.toml`.
- lead, 2026-05-18: Ran `npm run build -w @machtblick/bundestag`. Client, server, and prerender completed successfully.
- visibility, 2026-05-18: Pre-deploy check passed against generated HTML for `votes/index.html`, `en/votes/index.html`, and `en/motions/326191/index.html`.
- visibility, 2026-05-18: Verified title, descriptions, canonical URLs, `hreflang`, Open Graph, X card metadata, robots meta, JSON-LD, JSON alternates, `robots.txt`, `llms.txt`, API catalog, `_headers`, sitemap, manifest icons, favicons, and `og-image.png` at 1200 by 630. Blocking issues: none.
- lead, 2026-05-18: Tightened crawler policy so GPTBot, ClaudeBot, Google-Extended, Applebot-Extended, and CCBot are denied for training while user-requested retrieval bots stay allowed.
- visibility, 2026-05-18: Rebuilt `@machtblick/bundestag` and reran the visibility preflight with the stricter crawler policy. Result: PASS.
