# 110: Round two, correctness, structure, a11y, perf, onboarding

## Goal

Second full-app sweep on the post-109 tree, six fresh audit lenses. Unlike 109, not everything here is byte-neutral: wave B deliberately CORRECTS wrong public-facing numbers, wave C deliberately shrinks payloads, wave E adds a11y affordances. Each wave has its own proof standard. No deploy; dev verification only.

## Findings inventory (from six read-only audits, all verified with file:line)

- BUG-1 HIGH: majorityChoice counts absences as party position; loyal voters flagged as defectors when absences dominate; tie semantics diverge across three surfaces (majorityChoice.ts + vite-data/members.ts dup vs materialized position vs partyDetail partyVote)
- BUG-2 MED: routes/parties/$id/index.tsx (+en twin) read parent loader unguarded, crash instead of NotFound on cold prerender-fallback
- BUG-3 MED-LOW: translations.ts overlayVote drops DE fallback per-field except cleanTitle
- BUG-4 LOW-MED: materializeHandzeichenTallies stops seat collection at 6 parties; a 7th group gets 0 seats silently
- BUG-5 LOW: db/materialize-derived-data.ts hardcodes term_id = 21 (x3), violating 109's own rule
- ROUTES: DE/EN twins ~83% mechanical duplication, ~430-520 addressable lines (head() builders x4 ~50 lines each duplicated verbatim, list-route bodies, validateSearch, leaf redirects); 2 pure modules misfiled in hooks/; speech-texts query inlined 3x bypassing useSpeechTexts
- A11Y-1 HIGH: PartyHistoryChart zero non-visual path (sr-only per-term list + aria-hidden chart)
- A11Y-2 MED: FilterSheet focus trap leaks via search input (selector only queries button)
- A11Y-3 MED: FilterPill/MemberFilterPill nested interactive controls
- A11Y-4 MED: SpeechEntry role=button wraps real links; mirror VoteCard stretched-link pattern
- A11Y-5 MED: Hemicycle aria-label hardcoded German, no numbers
- PERF-P0: votes list SSRs twice (mobile+desktop DOM), 30 eager cards each, 630-circle hemicycle per card = 5.77MB HTML entry page
- PERF-P1a: recharts (102KB gz) eager on party detail, one import site
- PERF-P1b: EN speeches force-loads full ~13MB gz text corpus with empty query (speechesStatic.ts:97 locale==='en' condition)
- PERF-P2: react-markdown/micromark (~30KB gz) in home bundle for **bold** on cards; VoteHemicycle seats not memoized
- PERF-P3: member photos 20MB jpg/png unoptimized (267KB PNGs as avatars); no font preload for Fraunces
- LEFTOVERS: parseSex dup x2, isRelatedDebate dup x2, parties.ts O(n) slug reverse lookup (vite-data has PARTY_TO_SLUG pattern), badge/card/Link single-importer inlines, 3 stale mock.md symbol mentions, awFetchWithRetry needless export
- DOCS-1 BLOCKER: no db bootstrap instructions (cold clone cannot run)
- DOCS-2 BLOCKER: no LICENSE + README forbids derivatives while CONTRIBUTING welcomes PRs (POLICY, operator decides)
- DOCS-3: no .env.example (vars: CLOUDFLARE_* deploy-only, MACHTBLICK_DB optional, APP_STORE_CONNECT_* ios-only)
- DOCS-4: no engines field (root package.json, >=22)
- DOCS-5: CLAUDE.md + AGENTS.md list nonexistent packages/ui and db/schema.ts (real: db/schema/)
- DOCS-6: plans/ no README (141 files, dup numbers)
- DOCS-7: prompts/auto-refresh.md + systemd refresh undiscoverable from README
- DOCS-8: no apps/ios README (signing, fastlane vars, simulator-no-signing note)
- SEC: real Cloudflare token in gitignored .env; confirm never in git history, rotate if unsure (operator)

## Waves and proof standards

| Wave | Content | Proof |
|---|---|---|
| A | routes dedup, hooks moves, server leftovers, misc inlines | byte-identical build artifacts + prerendered HTML head unchanged |
| B | BUG-1..5 fixes | tsc green; artifact diff shows ONLY expected fields (loyalty/defection/partyMajority, party tallies); manual spot-check of 2-3 known votes |
| D | DOCS-1,3..8 (DOCS-2 waits on operator) | docs only, accuracy re-checked against tree |
| E | A11Y-1..5 | visual parity (tester screenshots), aria assertions |
| C (after A+E land) | PERF-P0,P1a,P1b,P2,P3 | tester full sweep, HTML size measured before/after, visual parity |

## Contracts

- Wave A must not change any rendered byte: head() builders parameterized by locale must reproduce the exact same meta/JSON-LD strings both locales. lib/routeHeads.ts is the new home; lib/searchParams.ts for guards.
- Wave B majorityChoice fix: align with ETL position() semantics (yes/no/abstain plurality, null on tie); defection/loyalty logic skips null. Prefer reading materialized position where available over recomputing.
- File ownership: waveA-frontend = routes/**, lib/routeHeads+searchParams, views route-body shells, hooks moves; waveB-backend = src/server/** + vite-data/members.ts; waveB-plumber = db/materialize-derived-data.ts + re-run + BUG-4/5; waveD = docs only; waveE = views/components a11y files (NOT routes/, NOT VotesList.tsx which is wave C's)
- Wave C's VotesList restructure must keep the snap-feed UX on mobile and columns on desktop, one DOM render.

## Status

| Item | Owner | Status |
|---|---|---|
| Wave A routes+leftovers | frontend | done |
| Wave B server bugs | backend | done |
| Wave B materialize bugs | plumber | done |
| Wave D docs | general | done (DOCS-2 excluded, waiting) |
| Wave E a11y | frontend2 | done |
| Wave C perf | frontend | done |
| DOCS-2 license | operator decision | done: AGPL-3.0 |
| Round-1 tester sweep | tester | done: 12/12 PASS |
| iOS null partyMajority fix | ios | done |
| Data fixes 1,2,6 | plumber | done (affiliations term-scoped, FDP/BSW summary purge + guard, mast-katja portrait nulled) |

## Log

- lead 2026-07-12: plan created from six round-2 audits. Dispatching waves A, B(x2), D, E in parallel (disjoint files); C follows A+E.
- waveB-plumber 2026-07-12: BUG-4+5 fixed in db/materialize-derived-data.ts only. BUG-5: three term_id=21 literals parameterized via CURRENT_TERM imported from apps/bundestag/src/server/term.ts (same pattern as build/shared.ts; chose importing over moving the constant to db/term.ts because the re-export would touch apps/src/server while two agents work there in parallel, and term.ts is a dependency-free one-liner so pulling it into the root tsc program is harmless; root tsc green). BUG-4: removed the seats.size>=6 early break, every party across the 20-vote window is now collected. Data reality: 8 distinct parties on non-namentlich term-21 rows (6 namentlich ones + BSW, FDP); 13 handzeichen rows have members=0 (BSW 7, FDP 6), all expected since neither has WP21 namentlich presence, so the break was never live-truncating. Re-ran db:materialize against backup /tmp/mb-sqlite-backup-round2.sqlite: 0 changed rows in vote_party_summaries, vote_debate_groups, speech_vote_links; second run dump sha256 identical (8ac99403...74f7), idempotent. No JSON regeneration needed (zero data changes). plumber.md handzeichen section updated.
- waveD 2026-07-12: DOCS-1,3..8 done, docs and metadata only, zero source changes, DOCS-2 untouched. Created: db/README.md (bootstrap: db:migrate gives empty schema, full regen impractical, snapshot via feedback@machtblick.de), .env.example (CLOUDFLARE_*, MACHTBLICK_DB, DIP_API_KEY optional with public fallback, APP_STORE_CONNECT_API_KEY_ID/API_ISSUER_ID/API_KEY_CONTENT plus TESTFLIGHT_PUBLIC_GROUP which beta_local ENV.fetch-requires), plans/README.md, apps/ios/README.md (verified against fastlane/Fastfile: scheme Machtblick, soli.Machtblick, team Q9U8224WWM). Edited: README.md (db/README link, no-.env-needed line, Scheduled data refresh section pointing at prompts/auto-refresh.md + scripts/systemd/), root package.json (engines node >=22), CLAUDE.md + AGENTS.md (plumber db/schema.ts -> db/schema/, packages/ui block removed, layout gains build/ and src/components/, etl comment now mentions _shared/_oneshot, prerenderPaths reference moved to apps/<app>/build/prerenderPaths.ts). Already correct, left alone: npm-only claims (no pnpm/yarn mentions), radius token values in globals.css, README "shared root packages" (db and etl workspaces exist). Verified: zero U+2014/U+2013 in touched files, package.json parses, npx tsc --noEmit green. NOT committed. FINDING for wave A/scribe: root .gitignore bare `build` entry ignores apps/bundestag/build/, the relocated generators (prerenderPaths.ts, sitemap.ts, ...) are currently untracked and would be missing from a cold clone; needs a .gitignore fix before commit.
- lead 2026-07-12: DOCS-2 resolved by operator: AGPL-3.0. LICENSE added (official gnu.org text), README rights section rewritten (use/modify/self-host permitted, copyleft), CONTRIBUTING gains inbound=outbound sentence. Also fixed the waveD-flagged .gitignore bug: bare `build` entry ignored apps/bundestag/build/; now `build/` + `!apps/bundestag/build`, generators visible to git. Round-1 tester: 12/12 PASS, no regressions, no console errors (logged in plan 109).
- waveB-backend 2026-07-12: BUG-1 fixed. majorityChoice.ts now maps the materialized position column (yes/no/abstain -> ja/nein/enthalten, mixed -> null), exported as `PartyMajority | null`; all consumers updated (memberDetail, members, voteDetail; vite-data/members dup deleted in favor of the shared fn). Null majority means: no defection, not loyalty-eligible, `partyMajority: null` in MemberVoteRow (type changed from `MemberVoteChoice | ''`; MemberVoteCard already guards truthy, no view edits needed). Also fixed the SAME 4-bucket logic dup in vite-data/votes.ts (outside the listed file set, but it generates the shipped vote JSON defector lists; without it the fix never reaches /votes/*.json). Proof against db/machtblick.sqlite: 107 of 3964 summary rows diverge (58 absent-plurality, 49 ties). Spot-checks for dev: vote 2025-11-13-978 (Tierarzneimittel/Apothekengesetz), B90/Gruene yes 0 no 41 abstain 0 absent 44 and Die Linke abstain 28 absent 36; old logic called both majorities 'nicht_abgegeben' and flagged all 69 unanimous voters as Abweichler. Also 2026-06-11-1007 (KFOR, fraktionslos absent-plurality) and tie rows like 2026-06-11-1006 (Eufor Althea, fraktionslos 1/1/0 -> null). Artifact diff vs /tmp/mb-baseline-artifacts: EXACTLY api/members.json (69 members, only field `loyalty`, e.g. hofreiter-anton 0.9796 -> 1.0), 152 members/*.json (only loyalty/defections/history.defected/history.partyMajority), votes/2025-11-13-978-*.json (defectors 69 -> 0), same three surfaces under en/; parties, motions, speeches, sitemap byte-identical. BUG-2: parties/$id/index.tsx + en twin guard `data ? <PartyDetailShell/> : null`. BUG-3: overlayVote in translations.ts gained per-field DE fallback (topic/subject/summary/summarySimplified/summaryDetail); same for the ternary overlays in vite-data/votes.ts (leanVotes + fullVote); no sibling overlay fns exist. Data reality: 2053/2104 EN vote_translations rows have >=1 NULL among those fields, but ZERO rows where EN is NULL while DE has content, so no artifact change today, robustness only. Leftovers: parseSex exported from members.ts next to MemberSex (memberDetail + demographics); isRelatedDebate exported from speechVoteSql.ts (voteDetail + antraege + vite-data/votes, was 3 inline copies); parties.ts slug lookup uses the existing PARTY_SLUG map from lib/parties (no new PARTY_TO_SLUG export needed, PARTY_SLUG IS party->slug and SLUG_TO_PARTY derives from it); vite-data/parties.ts imports both shared maps, local copies deleted (byte-identical output, verified). tsc green app+root. Scratch proof script deleted. NOT committed.
- waveE 2026-07-12: A11Y-1..5 done, tsc green, NOT committed. A11Y-1 PartyHistoryChart: sr-only ul (one li per term, "{termLabel}: {pct}%", locale decimal comma) labeled via new copy key shareOfBundestagByTerm (de+en); aria-hidden wraps ONLY the Recharts container, PartyHistoryEventStrip keeps its real text accessible. A11Y-2 FilterSheet trap selector broadened to 'button:not([disabled]), input, select, textarea, a[href]'. A11Y-3 FilterPill+MemberFilterPill: outer button replaced by a styled span (pillRef carries border/padding/hover/active bg) containing two real sibling buttons: open button with stretched before pseudo, reset button relative z-10; menu positioning and outside-click checks moved to pillRef so popup geometry is unchanged; MemberFilterPill reset aria-label localized via existing resetFilter key (was hardcoded DE). A11Y-4 SpeechEntry: role=button article replaced by plain relative article + absolutely positioned stretched button (button, not a: onOpen opens the Reader overlay) with aria-label from new copy key openSpeechOf joined with voteTitle when present; inner links keep z-10, dead stopPropagation handlers removed. A11Y-5 Hemicycle: svg aria-hidden replaces hardcoded German aria-label (PartiesList already renders totals and per-party seats as text, confirmed). Focus-trap test: /speeches/ mobile sheet (the only sheet with a searchable group), focus search input, Shift+Tab x4 and Tab x40 all stay inside the dialog, Escape returns focus to the FAB. Playwright interaction suite 20/20 PASS: pill Enter opens listbox, Tab from pill lands on reset, Enter on reset clears; no button-in-button or role=button nesting left in DOM; SpeechEntry Enter/Space/mouse all open Reader, speaker link still navigates above the stretched button. Visual parity: 10 before/after screenshot pairs at identical viewports (speeches idle/menu/active-member, parties hemicycle, party history spd, votes desktop idle/menu/active, active FilterPill with reset x close-up, votes mobile idle/sheet-open); 8 byte-identical incl. every restructured state; the 2 votes-mobile pairs differ by exactly 3 near-white pixels (max channel delta 16), proven renderer noise: re-shooting byte-identical code reproduces the same drift.
- waveA-frontend 2026-07-12: S1-S8 + leftovers done, tsc green, NOT committed. Net -434 lines (-1273/+839) across the wave A file set. Created: lib/routeHeads.ts (voteDetailHead/memberDetailHead/partyDetailHead/motionDetailHead + shared listHead feeding votesListHead/motionsListHead/membersListHead/partiesListHead/speechesListHead; breadcrumb names via copy[locale].navX, all other strings copied verbatim into locale ternaries INSIDE routeHeads.ts, not lib/copy, because the templates interleave interpolation and rewriting them into copy keys risked byte drift), lib/searchParams.ts (str/nonEmptyStr/oneOf/pastFirstPage helpers; validateVotesSearch/validateMotionsSearch/validateMembersSearch/validateSpeechesSearch/validateMemberLineSearch + speechesLoaderDeps; vote-detail tab validation stays in the two $id routes since they import VoteDetail anyway), lib/distinctSorted.ts, views/votesList/VotesRouteBody.tsx, views/antraegeList/AntraegeRouteBody.tsx, views/membersList/MembersRouteBody.tsx, views/redenSearch/RedenRouteBody.tsx (each takes `from` as a DE|EN route-id union; useLoaderData/useSearch/useNavigate({from}) type-check fine on unions), views/memberDetail/MemberSpeechesTab.tsx (+ copy key noSpeechesTerm, exact strings 'Keine Reden in WP21'/'No speeches in term 21'). Moved (git mv): hooks/memberSpeechGroups.ts and hooks/debateThread.ts to lib/ (5 importers updated). Deleted: components/ui/badge.tsx, components/ui/card.tsx, components/Link.tsx (single importers PartyBadge/PartyHistoryEmpty/MemberDetailTabs, inlined with exact same cn() input strings and data-slot attrs). Edits: MOTION_STATUS_BUCKETS moved to lib/motionStatus.ts; antragProposerKey and isPresidium unexported (single-file); useMemberSpeeches + useSpeechBody now consume useSpeechTexts (useSpeechBody return narrowed to { data?: { text } }, its only consumer Reader reads body.data?.text; DE-fallback timing behavior preserved, render-identical through all loading states); distinctSorted used for 4 facet derivations in the three filter hooks; mock scrubs in redenSearch.mock.md (searchSpeeches gone from server/speeches.ts), sponsorStrip.mock.md (DefectorRow/SpeechRow -> SpeechEntry), speeches.mock.md (StanceText line removed, SpeechEntry modes line reworded). Deliberately skipped as not line-reducing: redirect leaf twins (createFileRoute literal dominates), parties list body (2 lines), votes leaf twins, detail-route component fns (S1 scope was head only). Server-fn call shapes left EXACTLY as-is per byte-neutrality rule (DE positional vs EN {..., locale} object on getVote/getMember/getParty/getAntrag; listMembers takes no locale on either side). Preserved DE/EN asymmetries, verbatim, flagged for a later intentional fix: (1) motions detail links: DE canonicalLink(path,{englishAlternate:hasEnglishTranslation}) vs EN unconditional canonicalLink(path); (2) vote detail JSON-LD about[].url uses un-prefixed /motions/ on BOTH locales (EN page links DE motion URLs); (3) EN vote result word mapped (Passed/Rejected/Decided) + lowercased in Event description while DE uses raw v.result. Byte-neutrality proof: wave B was editing src/server+vite-data in parallel (md5 fingerprints diverged mid-wave), so a naive before/after diff was invalid; instead the repo was frozen into two /tmp copies at the same instant, ctrl = wave-B-state + wave-A-inverted (27/31 restored files byte-matched the git index, remaining 4 verified against HEAD or shown to differ only by my exact edits), after = wave-B-state + wave A. Both built (exit 0), then compared: 3867/3867 dist JSON byte-identical; 6451/6451 prerendered HTML byte-identical after normalizing ONLY (a) /assets/ chunk name+hash refs, (b) modulepreload tag lists, (c) the dehydrated router u: build timestamp (differs between ANY two builds); zero other file diffs. Meta, JSON-LD, dehydrated loader data, and DOM: unchanged to the byte. globals.css shrank 863 bytes (dead badge/card variant utilities). Chunk count 111 -> 112.
- waveC 2026-07-12: PERF-P0,P1a,P1b,P2a,P2b,P3a,P3b done. tsc green, full build green incl. both contract scripts, NOT committed. P0: VotesList now renders ONE list (single <main>, responsive classes: mobile keeps h-[calc(100svh-96px)] snap-start snap-always px-m pt-l, desk gets h-auto px-0 pt-m pb-m inside desk:mx-auto desk:max-w-3xl desk:px-l; d-{id} duplicate ids gone, merged wrapper keeps id={v.id}); EAGER_CARDS 30->6 (measured: 3 wrappers intersect the 1440x900 fold, 6 = 2x coverage; useNearViewport 1500px streaming unchanged, verified cards stream on scroll). index.html 5,767,641->991,803 raw (-83%), 536,408->130,828 gz (-76%), circles 38,299->3,828; en/index.html 5,753,217->982,582 raw; target <1.5MB beaten. Other feeds audited: members/motions/speeches/parties render once already. P1a: PartyHistoryChart React.lazy from PartyHistoryPanel; recharts chunk 100,110 gz async and NOT modulepreloaded by any page; PartyDetailShell chunk 102,639->2,928 gz. Restructure beyond brief: prerender SSRs the Suspense FALLBACK (lazy does not resolve during prerender), which would have dropped wave E's sr-only list from static HTML, so the ul moved from the chart into the eager Panel (helpers extracted to views/partyDetail/partyHistoryPoints.ts); prerendered party HTML verified to keep the localized list (DE "Sitzanteil je Wahlperiode" / EN "Seat share by parliamentary term", 6 li) while the chart svg now client-loads into the reserved 320px box (height 320 before and after chunk load, no shift; deliberate divergence: chart markup no longer in prerendered HTML). P2b: VoteHemicycle memo + useMemo(hemicycleSeats keyed totalMembers); PartyDonutRow + VoteDistributionDonut memo; vote detail legend verified live: Ja click 137 full/493 dimmed, Nein 163 dimmed, re-click clears, aria-pressed toggles. P1b root cause: /en/speeches-meta.json (EN excerpts, 1190 translated + 11057 DE-fallback matching the shard fallback) already existed but loadSpeechMeta always fetched DE meta, so the locale==='en' texts condition was compensating; fix = loadSpeechMeta(locale) with per-locale cache + texts only when terms.length>0; EN /speeches/ empty query now downloads ZERO shards (was 4 = 36.3MB raw / 12.9MB gz); search still loads them on demand (verified: 'climate' -> 4 shards, 229 results; Reader opens with full text). Deliberate divergence: EN excerpts are now the meta's 160-char word-trimmed form (same presentation as DE) instead of 220-char raw slices. useMemberSpeeches queryFn updated to () => loadSpeechMeta() (kept DE meta + voteTitleEn localization path, unchanged behavior). P2a: MarkdownInline rewritten from react-markdown to a CommonMark star-emphasis delimiter-stack parser (links, **strong**, *em*, partial delimiter runs, rule-of-3, NUL->U+FFFD); parity proven with renderToStaticMarkup over ALL 4389 real summaries (votes + antrag descriptions + party position summaries, DE+EN): 4388/4389 byte-identical, only divergence = raw HTML tags (old skipHtml drops, new escapes as text; zero corpus occurrences). Corpus scan found nested ***x* y** and unbalanced **x* forms that a naive regex mishandles, hence the real algorithm. micromark now lives only in the motions/Reader async chunk (37.8KB gz), absent from all 22 home modulepreloads; Markdown.tsx untouched. P3a: sharp added as devDependency; fetch-member-photos.mjs gained rotate+resize(width<=320)+recompress step (jpeg mozjpeg q80, png palette q80, rewrite only when smaller so reruns converge); corpus 19,043,669->10,452,951 bytes (-45%), 619 jpg + 15 png filenames/formats unchanged, manifest identical, avatars eyeballed crisp at 320w on /members/. P3b: rel=preload for /fonts/fraunces-latin-600-normal.woff2 in __root head (fonts are unhashed public/ paths, verified in built HTML + dist/client/fonts/); 700 deliberately NOT preloaded: no CSS rule requests weight 700 (only the @font-face decl exists), so the browser never fetches it and preloading would be a dead 25KB + console warning. Playwright on :5174: 24 distinct checks all PASS across votes desktop 1440x900 (3 visible cards, pills, hemicycles, bold summaries) / votes mobile 390x844 (scroll-snap mandatory, full-viewport cards, snap-to-boundary, FAB + sheet) / vote detail legend / party history lazy (sr-only present before chunk) / EN+DE speeches / members avatars; screenshots in session scratchpad. Flag for designer/lead: votesList.mock.md clamp-mechanics note still mentions the retired d-{id} desktop wrappers (mock is designer-owned, not edited).
- tester-round2 2026-07-12: 13/13 PASS on http://127.0.0.1:5174, Chromium headless, desktop 1440x900 + mobile 390x844. (1) votes desktop: 223 cards, 7 full-render eager, 23 after scrolling first day-group (lazy streaming works), hemicycle+donut+summary render, card click navigates. (2) votes mobile: html scroll-snap-type y mandatory, full-viewport cards (~748px), FAB opens sheet, option applies (FAB shows "Filter · 1"), Escape closes; focus trap: Shift+Tab x4 from the speeches sheet search input stays in dialog (votes sheet has no searchable group), Escape refocuses FAB. (3) UNIFIL detail: Ja legend click dims 119 dots, re-click clears, aria-pressed toggles; Reader from debate list via Mehr anzeigen -> Ganze Rede lesen (only reachable with text shards unavailable, tested by aborting speeches-search-*; locally shards load instantly and the bubble expands inline instead), footer next/prev cycles Wortmeldung 1->2->1, Escape closes. (4) BUG-1: vote 978 zero defector UI (no Abweichler anywhere), hofreiter-anton Linientreue 100% / 0 Abweichungen (was 97.96% + false defections), merz 100%. (5) party detail gruene: h1 paints before chart chunk (delayed 1.5s via route), recharts mounts into the 320px reserved box, belowShift=0px, Geschlossenheit 99% + Anwesenheit 89% present. (6) votes feed: 32 strong/em from MarkdownInline (note: speech excerpts are plain text, the markdown surface is vote/antrag summaries); /speeches/ Klima search highlights, Reader opens by mouse AND by focus+Enter on the stretched button, speaker link navigates independently. (7) /en/speeches/ empty query: ZERO speeches-search requests, excerpts render; 'climate' loads exactly 4 speeches-search-en-* shards, 8 results. (8) /en/votes/ + EN 978 + EN merz: no undefined/NaN/[object/empty headings. (9) FilterPill: Enter opens listbox, select, Tab lands on sibling reset (aria-label Filter zurücksetzen), Enter clears; mouse select+reset clears too (exercised the first pill; no pill labeled Partei/Fraktion on /votes/). (10) 1205 avatars, 3 eyeballed crisp (abdi/lange/yuksel), zero local 404s/broken imgs. FINDING (pre-existing data, not round-2): mast-katja has no local photo and her members.picture_url wikimedia URL returns upstream 404, broken avatar on /members/; for plumber. (11) sr-only ul[aria-label="Sitzanteil je Wahlperiode"] with 6 li incl. values. (12) dark/light x DE/EN: dataset.theme + bg correct (rgb(0,0,0)/rgb(255,255,255)). Console errors: zero across all flows (only deliberate net::ERR_FAILED from the flow-3 shard abort). No commit, no deploy, spec deleted.
- lead 2026-07-12 (round 3): security review findings fixed inline: MarkdownInline now allowlists href schemes (https?:, /, ./, #; unsafe links render as plain text), jsonLd() escapes the less-than character as a unicode escape, closing the pre-existing script-tag breakout, test-results/ gitignored. CSS.escape hardening judged unnecessary (ids provably [a-z0-9-] from ETL slugify). tsc green.
- visibility 2026-07-12: full audit of dist/client (6450 pages) PASS, no blockers, nothing fixed. Verdicts on the three preserved asymmetries: (a) DE motions conditional englishAlternate is latent-broken, all 977 built motions have hasEnglishTranslation=true so zero output impact today, but the first untranslated motion yields non-reciprocal hreflang (EN twin is always prerendered and always self-declares en), Google then drops the cluster; fix = make routeHeads.ts:246 unconditional `...canonicalLink(path)` and delete the englishAlternate option from seo.ts:46-58. (b) EN vote Event.about[].url emits DE /motions/ URLs; not invalid but breaks entity-linking with the EN Legislation node's @id/url; fix = withLocale at routeHeads.ts:95. (c) EN result-word mapping is CORRECT localization, keep; mapping is total for all built data (223 namentlich: only angenommen/abgelehnt; lone 'unterbrochen' DB row not in term window). Extra minor: Person url (routeHeads.ts:156) and PoliticalParty url (:207) lack the trailing slash their own @id and canonical carry. Verified healthy: 5x2 sampled heads (title/desc lengths, canonical, reciprocal hreflang de/en/x-default, OG+twitter absolute 1200x630 images, JSON-LD parses, robots max-image-preview:large), zero noindex in 6450 files, sitemap 3856 entries all self-canonical (400-file sample), data-derived lastmod, no redirecting/query URLs, votes.xml valid Atom 50 entries, llms.txt + api-catalog + mcp/agent-skills JSON consistent with dist and iOS Endpoints.swift, homepage P0 keeps all 223 vote titles+links SSR'd (only summaries lazy, harmless given detail pages are the canonical targets). Audit-tooling note: prerendered HTML contains NUL bytes from the dehydrated payload (pre-existing on prod), plain grep silently fails, use python or LC_ALL=C grep -a.
- ios-round3 2026-07-12: fixed the confirmed iOS decode break from wave B's `partyMajority: null` (39 history entries across knodel-sieghard 14, schmidt-jan-wenzel 11, seidler-stefan 14; verified against apps/bundestag/public/members/*.json). MemberDetailPayload.HistoryEntry.partyMajority is now `String?`; whole-payload decode no longer fails, ErrorStateView gone on those member pages. Consumers audited via full grep: UI/MemberVoteRelation.swift computed property now `entry.partyMajority.flatMap(BallotChoice.init)` (nil majority = no donut highlight, extra top padding, and MemberVoteConnector's existing `let partyMajority` guard skips the party-line connector); MemberVoteConnector already took `BallotChoice?`, unchanged; showsLineStatus keys off `defected != nil` as audited, and the 39 real entries split into defected=null (line status hidden) and defected=false only where choice=nicht_abgegeben (hidden via the choice check), so no wrong "held the line" chip can render. scripts/MemberPayloadContractCheck.swift gained a third fixture with `"partyMajority":null,"defected":null` plus preconditions (partyMajority nil, showsLineStatus false), compiled by ios-build.yml/ios-testflight.yml, so CI catches a regression to non-optional. check-localizations, check-more-ui-contract, check-settings-content-parity all pass; Swift compile gate is CI on macOS. NOT committed.
- lead 2026-07-12 (round 3, cont): integration-review blocker fixed at the wire layer: build/jsonEndpoints.ts members writer maps partyMajority null to empty string, keeping shipped iOS apps decoding (Swift-side String? fix + null fixture landed separately, belt and braces). Follow-ups applied: awFetchWithRetry unexported, build/shared.ts PARTY_SLUGS now re-exports lib/parties PARTY_SLUG (drift eliminated), root test-results/ was already gitignored (reviewer raced the earlier fix). SEO fixes 1-3 applied in routeHeads.ts + seo.ts: hreflang en alternate now unconditional (englishAlternate option deleted; server hasEnglishTranslation field left in payload, now unused by heads), EN vote JSON-LD about[].url localized via withLocale, Person/PoliticalParty url gained trailing slash matching @id. speeches.mock.md staleness (defect 5) pending. tsc green.

## Data follow-ups (documented, out of today's scope; from the round-3 data-quality audit)

- EN speech-translation backlog: 1,132 untranslated speeches across 94 visible votes render fully German speech tabs on /en/ (backlog reaches to 2026-02, so the worker is capped or skipping, not lagging). Needs a translations-worker throughput/coverage fix + a catch-up run.
- WP20 duplicate votes: 125 vote pairs exist under two id schemes (bt20-* and bare) with full duplicate ballot sets (~92k rows). Latent while only term 21 renders; must be deduped (one-shot + importer guard) before any historical-terms feature (see the b20.machtblick.de idea).
- 18 visible handzeichen votes (incl. Haushaltsgesetz 2026) missing generated summaries; known Drucksache-extraction gap in the description picker for Einzelplan/Schlussabstimmung rows.
- 12 WP21 Gesetzentwuerfe missing clean_title + EN translation (one dated 2025-07-11, so the worker permanently skips some rows); 5 antraege have no content at all (332795, 334134, 334864, 335940, 336303).
- 52 sitting members have no portrait upstream (initials fallback, acceptable); iOS hardcodes .jpg while 15 photos are .png (avatars fall back to initials on iOS, pre-existing, fix by resolving via manifest).
- plumber-round3 2026-07-12: data-quality fixes 1, 2, 6 done, DB backed up to /tmp/mb-sqlite-backup-round3.sqlite first, NOT committed. Fix 1 (root cause of fix 2): etl/bundestag-affiliations/ was running loadPartyRuns over ALL terms' vote_members and inserting with the term_id-default-21 + periodStart defaults, so 1,108 WP20-only members carried OPEN term-21 affiliations (that garbage fed handzeichen/write.mjs's activeParties filter, which is exactly how the FDP/BSW summary rows got in), 47 rows had valid_to < valid_from (2025-03-25 first-run valid_from vs 2021 next-run dates), 40 rows carried 'DIE LINKE.'. runsFromVotes.ts now takes termId (votes.term_id filter) and normalizes labels via canonicalPartyToken from etl/_shared/parties.ts (raw kept when unmatched so fraktionslos survives); ingest.ts imports CURRENT_TERM, inserts explicit termId, deletes WHERE term_id = CURRENT_TERM. Confirmed the ingest is a deterministic full rewrite per term; it fetches two abgeordnetenwatch endpoints (candidacies-mandates period 161 + parliament-periods/161), both worked (11 mid-period changes, 0 unmatched). Re-ran npm run etl:affiliations (ingest + chained close-departed): 640 rows for 639 members (4,033 before), open term-21 = 630 exactly, 0 inverted-validity, 0 'DIE LINKE.', per-party open counts identical to party_seat_history term 21 (CDU/CSU 208, AfD 150, SPD 120, B90/Grüne 85, Linke 64, fraktionslos 3). Fix 2: the 13 spurious rows (FDP 6, BSW 7, all position='no' members=0, votes pp21-42-0, pp21-42-1, pp21-44-16 BSW-only, pp21-45-5-haushaltsgesetz-2026, pp21-47-5, pp21-58-0, pp21-63-0) are deleted by a new recurrence guard in materializeHandzeichenTallies() rather than a bare one-shot: DELETE current-term non-namentlich vote_party_summaries rows whose party is not in party_seat_history for CURRENT_TERM (skip when seat history empty, protects fresh DBs), runs before tallying on every db:materialize so it self-heals; first run purged exactly 13, second run 0, write-side activeParties simulation for 2025-11-25 now yields only the 6 seated groups. Fix 6: Wikidata Q1736645 has NO P18 claim anymore and the stored Commons URL 404s (file deleted upstream), so re-running portraits could never fix or re-break it (unmatched rows untouched); one-shot UPDATE nulled all four picture_* columns on mast-katja, initials fallback renders. Regenerated JSON endpoints via writeJsonEndpoints(); full recursive diff of apps/bundestag/public against pre-change snapshot: exactly 22 files, all in scope. 14 vote JSONs (7 votes x de/en) + api/votes.json x2: ONLY the FDP/BSW partySummaries entries removed, every other field byte-equal (dek/summary text unchanged, so no deriveDek drift); members/mast-katja.json x2 (four picture fields to null), motions/330644.json x2 (her signatory portraitUrl to null), speeches-people.json x2 (her entry dropped). api/members.json NOT in the diff: leanMembers party assignments unchanged, verified explicitly. Zero unexpected diffs. tsc green root + apps/bundestag. plumber.md updated: seatless-party purge note in the handzeichen-tallies section, term-scoping note in member_affiliations section, mast-katja precedent in the portraits section. Out of scope per brief: EN translation backlog, WP20 vote dedupe, missing summaries.
